## 2026-08-23

### 流式响应可视化面板（RequestLogDetailView）
- 位置：`frontend/src/views/RequestLogDetailView.vue` 的「响应（response_json）」折叠项下新增一个 `AccordionItem value="stream"`，仅当 `log.stream === true` 显示。
- 新组件目录 `frontend/src/components/stream/`：
  - `StreamCollapsibleBlock.vue`：高度过渡折叠块基础（v-model:open + ResizeObserver 同步高度）。复用 codex-base-ui `CollapsibleBlock` 范式，颜色替换成 Tailwind v4 token。
  - `StreamMarkdownBlock.vue`：流式 markdown 主文本（marked.parse + DOMPurify 白名单 + highlight.js/common 围栏高亮 + 流式尾巴脉冲光标）。双层降级（marked 抛错 → escaped text；DOMPurify 抛错 → 直接 escapeHtml）。
  - `StreamChunkTimeline.vue`：逐 chunk 时间轴（默认折叠在 StreamCollapsibleBlock 里），每行 #N + 状态徽标 + finish_reason + delta 预览 + click 展开看完整 chunk JSON。
  - `StreamResponsePanel.vue`：顶层整合 stats（chunks/usage/truncated）+ 思考过程折叠（reasoning 非空才显示）+ 主文本块 + 时间轴。
- 配套新工具 `frontend/src/lib/parseSSE.ts`：导出 `parseStreamBody(body)`、`extractResponseBody(response_json)`、`looksLikeSSE(body)` + 类型 `ParsedChunk`/`ParsedStream`/`StreamUsage`。容错：JSON.parse 失败的行原样落到 `parsed=false`，`data: [DONE]` 单独标记 isDone。
- 新依赖（用户拍板装三个）：`marked@^18.0.10` + `dompurify@^3.4.14` + `highlight.js@^11.12.0` + `@types/dompurify`。包体积影响：RequestLogDetailView 由 ~5 kB → 254 kB；其余视图不变。
- 验证：`vue-tsc -b` 全过（strict + noUnusedLocals + erasableSyntaxOnly），`vite build` 824 modules ~1.7s 跑通。
- 用户偏好冲突提醒：用户工作记录里写「对依赖谨慎、优先零依赖实现」（token 估算不引 tiktoken-go）。本次通过 AskUserQuestion 让用户拍板装三个生产依赖，不是绕过询问。
- 后续待办（用户没说做，先记下）：chunk 末尾二进制块（多模态图片）没单独可视化；route_attempts.first_byte_at 跟 response_json 不同表，TTFT 显示要做关联写入。

### 对话预览面板（StreamResponsePanel → ChatPreviewPanel）
- 用户拍板把「流式响应可视化」面板升级成 ChatGPT/Claude 风格的对话预览：渲染 `request_json.body` 里 `messages` 数组 + 响应结果拼成最后一条 assistant 消息。底部 stats + 逐 chunk 时间轴保留。
- 新组件目录 `frontend/src/components/chat/`（从 `backup/codex-base-ui/web/src/components/chat` 全部复制）：
  - `ChatView.vue`（改造：去掉 store 依赖，纯 props 驱动）+ `UserMessage.vue` / `TextBlock.vue` / `ThinkingBlock.vue` / `CodeBlock.vue` / `CollapsibleBlock.vue` / `ToolCallCard.vue` / `ShellTool.vue` / `EditTool.vue` / `ImageTool.vue` / `ErrorCard.vue` / `TodoListCard.vue` / `TaskItem.vue`。
  - 11 个 JS 版 SFC（无 `lang="ts"`）触发 TS7016，全部补 `lang="ts"` 与类型标注。`AxImage.vue` / `AxImageViewer.vue` 复制到 `frontend/src/components/ui/`。
- 辅助：`frontend/src/composables/useCollapse.ts`（JS → TS）+ `useGroups.ts`（仅改 `@/types` → `@/lib/chatTypes`）+ `frontend/src/lib/chatTypes.ts`（NormalizedMessage / ToolCall / PlanStep / CommandAction）。
- 新增 `frontend/src/components/stream/ChatPreviewPanel.vue`：解析 request_json.body 抽出 messages（容错 content 是字符串/多模态数组/数字三种形态）转 NormalizedMessage[]；`response_json.body` 解析后 reasoning → thinking 消息、content → stream/text 消息（done 时 id 加 `msg-` 前缀去掉光标）；用 computeGroups 分组给 ChatView 渲染。
- `RequestLogDetailView.vue` 把 `StreamResponsePanel` 换成 `ChatPreviewPanel`，传 `request_json` / `response_json` / `done` / `truncated` / `status_code`。
- 验证：vue-tsc 全过（strict + noUnusedLocals + erasableSyntaxOnly），vite build 跑通（RequestLogDetailView 由 254 kB → 288 kB，新增 chat 组件 + AxImage 全量复制进来）；用 Chrome MCP 实际跑通 17a44a37（deepseek-v4-flash user 含 vision marker）/ 4048fed2（hy3 user 发 "22" + assistant markdown 列表加粗）两条真实记录，渲染效果与设计一致。
- **旧 `StreamResponsePanel.vue` 现在无人引用**——可删（用户没明确指示，暂留）。

### 工具调用（tool_calls）补全
- 痛点：原 parseSSE 只解析 `delta.content` / `delta.reasoning_content`，`delta.tool_calls` 跨 chunk 累积全被丢掉，对话预览里工具调用卡片出不来（user 反馈）。
- `frontend/src/lib/parseSSE.ts` 升级：
  - 新增 `ToolCallDeltaFragment`（单 chunk 的增量片段，含 index / id / type / name / arguments 增量）。
  - `ParsedChunk['fields']` 增加 `toolCallsDelta?: ToolCallDeltaFragment[]`。
  - 新增 `ParsedToolCall`（聚合完成，含 id/type/name/argumentsRaw/argumentsParsed）。
  - `ParsedStream` 增加 `toolCalls: ParsedToolCall[]` 聚合结果。
  - `parseStreamBody` 主循环用 `Map<index, ParsedToolCall>` 按 index 累积拼接 arguments，途中 JSON.parse 实时尝试（拼到一半的自然失败留 null，最终完整时成功）。
- `frontend/src/components/stream/ChatPreviewPanel.vue`：`responseMessages` 累积完成后每个 `toolCalls[]` 转一条 `{kind:'tool_use', id: 'resp-tool-'+toolCallId, toolName, toolInput: argumentsParsed ?? argumentsRaw, toolResult: null, status:'done'}`。`useGroups.ts` 把相邻 tool_use 合并成 tool-group 渲染 `ToolCallCard`。
- `frontend/src/components/chat/ShellTool.vue`：加 `isMcp` 判断（toolName 含 `mcp` 且含 `__` / `.` / `/`），`label` 显示「MCP 工具」/ 标题显示 `mcp__server__tool` 名，模板里用「调用 {...}」替代「$ {...}」。`mcp__smart__status` 等 MCP 工具调用卡片可读。
- 验证：vue-tsc 全过；起 dev 5175 + Chrome MCP 打开真实 doubao 记录 27d571c5debeb641d8f67c6e9f26849a：
  - 用户消息「查看当前 有什么MCP 介绍他们」右气泡
  - 思考过程折叠「用户让我查看当前有什么MCP工具并介绍它们…」（43 字）
  - 工具调用卡片 `mcp__smart__status` 显示
  - 状态「已完成 · tool_calls」、chunks 47、parsed 46、状态码 200、usage（prompt=6277 / completion=186 / total=8385 / reasoning_tokens=62）
  - 底部 47 chunks ·逐 chunk 时间轴·已收到 [DONE]
- **ToolCallCard 分发兜底**：mcp__ 工具落 ShellTool（`isMcp=true`）；image* / view_image → ImageTool；edit / write / patch / file → EditTool；update_plan → TodoListCard；codex_error → ErrorCard（这部分是 reference 原样，未改）。

### 全面盘点 + 三格式补齐（P0/P1/P2 全修）
- 用户问"还有什么是你没有识别到的"，自查后列举 P0/P1/P2 缺口，用户勾选了全部修。
- **P0: parseSSE.ts 三格式解析**（参考 `plugins/vision_v2/stream.go` 的 feedChat / feedClaude / feedResponses）：
  - 协议探测 `detectStreamFormat(body)`：data 行里 `choices` → chat；`content_block_*` / `message_*` → claude；`response.*` → responses。
  - `parseChatStream`：原有 chat/completions 流；新增 `delta.refusal`（OpenAI 拒绝场景）+ 旧版 `delta.function_call`（非 tool_calls 的 function call 跨 chunk 累积）+ 多 choice 时把 choice[0] 之外的增量也收集到 fields（contentAccum 仍按 choice[0] 拼接避免混乱）。
  - `parseClaudeStream`：识别 `content_block_start/delta/stop` + `message_start/delta/stop` + `event:` 行（跳过）。`delta.type` 分支：`text_delta` → contentAccum；`thinking_delta` → reasoningAccum；`input_json_delta` → tool call arguments 累积（按 content_block index）。`message_stop` → isDone。
  - `parseResponsesStream`：识别 `response.output_text.delta/done` → contentAccum；`response.reasoning_*.delta/done` → reasoningAccum；`response.output_item.added` (function_call) → 开 tool call，`response.function_call_arguments.delta` → 累积 arguments，`response.output_item.done` → 完整 arguments 覆盖累积值；`response.completed` → isDone + usage。
  - 公共 `ToolCallAggregator` 类替三个分格式的累积逻辑。
- **P1: ChatPreviewPanel 请求 `function` role**：旧版 OpenAI function 结果（无 tool_call_id，靠 name 关联）建孤儿 tool_use 记录并渲染。
- **P1: refusal 字段**：parseSSE 加 `fields.refusalDelta`、`ParsedStream.refusalAccum`（当前未在 stats 显示，时间轴行已支持预览「拒绝：...」）。
- **P2: StreamChunkTimeline 死代码清理**：移除无用的 `lastIsTruncated: false` + 三元逻辑改 if；新增 `format` 徽标（chat/claude/responses 灰底色）。
- **P2: stats 行加协议徽标 + tool 调用数量**：`协议 chat` + `工具调用 1` 两个新指标。
- 验证：写临时 `parseSSE.test.tmp.ts`（后删）跑 4 个场景：
  1. chat+tool_calls（doubao 27d571c5 真实样本简化）：arguments 跨 chunk 拼接 + tool_call accumulator OK
  2. claude：text_delta / thinking_delta / input_json_delta 三个分支全 OK，content/思考/工具调用三段都对
  3. responses：output_text.delta + function_call_arguments.delta + output_item.done 覆盖逻辑 OK，response.completed 收尾 OK
  4. refusal 字段提取 OK
  - vue-tsc -b 全过；clean build（清 dist 后）通过（之前失败是 dist 残留）；CSS 总数不变；RequestLogDetailView 由 288 kB → 297.80 kB（+9 kB，三格式解析逻辑）
- dev server 验证：6236fcde 多轮 doubao 真实记录 → stats 行多了「工具调用 1」+「协议 chat」徽标；对话预览完整。
