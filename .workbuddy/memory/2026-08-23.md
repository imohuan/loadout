## 2026-08-23

### 流式响应可视化面板（RequestLogDetailView）
- 位置：`frontend/src/views/RequestLogDetailView.vue` 的「响应（response_json）」折叠项下新增一个 `AccordionItem value="stream"`，仅当 `log.stream === true` 显示。
- 新组件目录 `frontend/src/components/stream/`：
  - `StreamCollapsibleBlock.vue`：高度过渡折叠块基础（v-model:open + ResizeObserver 同步高度）。复用 codex-base-ui `CollapsibleBlock` 范式，颜色替换成 Tailwind v4 token。
  - `StreamMarkdownBlock.vue`：流式 markdown 主文本（marked.parse + DOMPurify 白名单 + highlight.js/common 围栏高亮 + 流式尾巴脉冲光标）。双层降级（marked 抛错 → escaped text；DOMPurify 抛错 → 直接 escapeHtml）。
  - `StreamChunkTimeline.vue`：逐 chunk 时间轴（默认折叠在 StreamCollapsibleBlock 里），每行 #N + 状态徽标 + finish_reason + delta 预览 + click 展开看完整 chunk JSON。
  - `StreamResponsePanel.vue`：顶层整合 stats（chunks/usage/truncated）+ 思考过程折叠（reasoning 非空才显示）+ 主文本块 + 时间轴。
- 配套新工具 `frontend/src/lib/parseSSE.ts`：导出 `parseStreamBody(body)`、`extractResponseBody(response_json)`、`looksLikeSSE(body)` + 类型 `ParsedChunk`/`ParsedStream`/`StreamUsage`。容错：JSON.parse 失败的行原样落到 `parsed=false`，`data: [DONE]` 单独标记 isDone。
- 新依赖（用户拍板装三个）：`marked@^18.0.10` + `dompurify@^3.4.14` + `highlight.js@^11.12.0` + `@types/dompurify`。包体积影响：RequestLogDetailView 由 ~5 kB → 254 kB；其余视图不变。
- 验证：`vue-tsc -b` 全过（strict + noUnusedLocals + erasableSyntaxOnly），`vite build` 824 modules ~1.7s 跑通。
- 用户偏好冲突提醒：用户工作记录里写「对依赖谨慎、优先零依赖实现」（token 估算不引 tiktoken-go）。本次通过 AskUserQuestion 让用户拍板装三个生产依赖，不是绕过询问。
- 后续待办（用户没说做，先记下）：chunk 末尾二进制块（多模态图片）没单独可视化；route_attempts.first_byte_at 跟 response_json 不同表，TTFT 显示要做关联写入。

### 对话预览面板（StreamResponsePanel → ChatPreviewPanel）
- 用户拍板把「流式响应可视化」面板升级成 ChatGPT/Claude 风格的对话预览：渲染 `request_json.body` 里 `messages` 数组 + 响应结果拼成最后一条 assistant 消息。底部 stats + 逐 chunk 时间轴保留。
- 新组件目录 `frontend/src/components/chat/`（从 `backup/codex-base-ui/web/src/components/chat` 全部复制）：
  - `ChatView.vue`（改造：去掉 store 依赖，纯 props 驱动）+ `UserMessage.vue` / `TextBlock.vue` / `ThinkingBlock.vue` / `CodeBlock.vue` / `CollapsibleBlock.vue` / `ToolCallCard.vue` / `ShellTool.vue` / `EditTool.vue` / `ImageTool.vue` / `ErrorCard.vue` / `TodoListCard.vue` / `TaskItem.vue`。
  - 11 个 JS 版 SFC（无 `lang="ts"`）触发 TS7016，全部补 `lang="ts"` 与类型标注。`AxImage.vue` / `AxImageViewer.vue` 复制到 `frontend/src/components/ui/`。
- 辅助：`frontend/src/composables/useCollapse.ts`（JS → TS）+ `useGroups.ts`（仅改 `@/types` → `@/lib/chatTypes`）+ `frontend/src/lib/chatTypes.ts`（NormalizedMessage / ToolCall / PlanStep / CommandAction）。
- 新增 `frontend/src/components/stream/ChatPreviewPanel.vue`：解析 request_json.body 抽出 messages（容错 content 是字符串/多模态数组/数字三种形态）转 NormalizedMessage[]；`response_json.body` 解析后 reasoning → thinking 消息、content → stream/text 消息（done 时 id 加 `msg-` 前缀去掉光标）；用 computeGroups 分组给 ChatView 渲染。
- `RequestLogDetailView.vue` 把 `StreamResponsePanel` 换成 `ChatPreviewPanel`，传 `request_json` / `response_json` / `done` / `truncated` / `status_code`。
- 验证：vue-tsc 全过（strict + noUnusedLocals + erasableSyntaxOnly），vite build 跑通（RequestLogDetailView 由 254 kB → 288 kB，新增 chat 组件 + AxImage 全量复制进来）；用 Chrome MCP 实际跑通 17a44a37（deepseek-v4-flash user 含 vision marker）/ 4048fed2（hy3 user 发 "22" + assistant markdown 列表加粗）两条真实记录，渲染效果与设计一致。
- **旧 `StreamResponsePanel.vue` 现在无人引用**——可删（用户没明确指示，暂留）。

### 工具调用（tool_calls）补全
- 痛点：原 parseSSE 只解析 `delta.content` / `delta.reasoning_content`，`delta.tool_calls` 跨 chunk 累积全被丢掉，对话预览里工具调用卡片出不来（user 反馈）。
- `frontend/src/lib/parseSSE.ts` 升级：
  - 新增 `ToolCallDeltaFragment`（单 chunk 的增量片段，含 index / id / type / name / arguments 增量）。
  - `ParsedChunk['fields']` 增加 `toolCallsDelta?: ToolCallDeltaFragment[]`。
  - 新增 `ParsedToolCall`（聚合完成，含 id/type/name/argumentsRaw/argumentsParsed）。
  - `ParsedStream` 增加 `toolCalls: ParsedToolCall[]` 聚合结果。
  - `parseStreamBody` 主循环用 `Map<index, ParsedToolCall>` 按 index 累积拼接 arguments，途中 JSON.parse 实时尝试（拼到一半的自然失败留 null，最终完整时成功）。
- `frontend/src/components/stream/ChatPreviewPanel.vue`：`responseMessages` 累积完成后每个 `toolCalls[]` 转一条 `{kind:'tool_use', id: 'resp-tool-'+toolCallId, toolName, toolInput: argumentsParsed ?? argumentsRaw, toolResult: null, status:'done'}`。`useGroups.ts` 把相邻 tool_use 合并成 tool-group 渲染 `ToolCallCard`。
- `frontend/src/components/chat/ShellTool.vue`：加 `isMcp` 判断（toolName 含 `mcp` 且含 `__` / `.` / `/`），`label` 显示「MCP 工具」/ 标题显示 `mcp__server__tool` 名，模板里用「调用 {...}」替代「$ {...}」。`mcp__smart__status` 等 MCP 工具调用卡片可读。
- 验证：vue-tsc 全过；起 dev 5175 + Chrome MCP 打开真实 doubao 记录 27d571c5debeb641d8f67c6e9f26849a：
  - 用户消息「查看当前 有什么MCP 介绍他们」右气泡
  - 思考过程折叠「用户让我查看当前有什么MCP工具并介绍它们…」（43 字）
  - 工具调用卡片 `mcp__smart__status` 显示
  - 状态「已完成 · tool_calls」、chunks 47、parsed 46、状态码 200、usage（prompt=6277 / completion=186 / total=8385 / reasoning_tokens=62）
  - 底部 47 chunks ·逐 chunk 时间轴·已收到 [DONE]
- **ToolCallCard 分发兜底**：mcp__ 工具落 ShellTool（`isMcp=true`）；image* / view_image → ImageTool；edit / write / patch / file → EditTool；update_plan → TodoListCard；codex_error → ErrorCard（这部分是 reference 原样，未改）。

### 全面盘点 + 三格式补齐（P0/P1/P2 全修）
- 用户问"还有什么是你没有识别到的"，自查后列举 P0/P1/P2 缺口，用户勾选了全部修。
- **P0: parseSSE.ts 三格式解析**（参考 `plugins/vision_v2/stream.go` 的 feedChat / feedClaude / feedResponses）：
  - 协议探测 `detectStreamFormat(body)`：data 行里 `choices` → chat；`content_block_*` / `message_*` → claude；`response.*` → responses。
  - `parseChatStream`：原有 chat/completions 流；新增 `delta.refusal`（OpenAI 拒绝场景）+ 旧版 `delta.function_call`（非 tool_calls 的 function call 跨 chunk 累积）+ 多 choice 时把 choice[0] 之外的增量也收集到 fields（contentAccum 仍按 choice[0] 拼接避免混乱）。
  - `parseClaudeStream`：识别 `content_block_start/delta/stop` + `message_start/delta/stop` + `event:` 行（跳过）。`delta.type` 分支：`text_delta` → contentAccum；`thinking_delta` → reasoningAccum；`input_json_delta` → tool call arguments 累积（按 content_block index）。`message_stop` → isDone。
  - `parseResponsesStream`：识别 `response.output_text.delta/done` → contentAccum；`response.reasoning_*.delta/done` → reasoningAccum；`response.output_item.added` (function_call) → 开 tool call，`response.function_call_arguments.delta` → 累积 arguments，`response.output_item.done` → 完整 arguments 覆盖累积值；`response.completed` → isDone + usage。
  - 公共 `ToolCallAggregator` 类替三个分格式的累积逻辑。
- **P1: ChatPreviewPanel 请求 `function` role**：旧版 OpenAI function 结果（无 tool_call_id，靠 name 关联）建孤儿 tool_use 记录并渲染。
- **P1: refusal 字段**：parseSSE 加 `fields.refusalDelta`、`ParsedStream.refusalAccum`（当前未在 stats 显示，时间轴行已支持预览「拒绝：...」）。
- **P2: StreamChunkTimeline 死代码清理**：移除无用的 `lastIsTruncated: false` + 三元逻辑改 if；新增 `format` 徽标（chat/claude/responses 灰底色）。
- **P2: stats 行加协议徽标 + tool 调用数量**：`协议 chat` + `工具调用 1` 两个新指标。
- 验证：写临时 `parseSSE.test.tmp.ts`（后删）跑 4 个场景：
  1. chat+tool_calls（doubao 27d571c5 真实样本简化）：arguments 跨 chunk 拼接 + tool_call accumulator OK
  2. claude：text_delta / thinking_delta / input_json_delta 三个分支全 OK，content/思考/工具调用三段都对
  3. responses：output_text.delta + function_call_arguments.delta + output_item.done 覆盖逻辑 OK，response.completed 收尾 OK
  4. refusal 字段提取 OK
  - vue-tsc -b 全过；clean build（清 dist 后）通过（之前失败是 dist 残留）；CSS 总数不变；RequestLogDetailView 由 288 kB → 297.80 kB（+9 kB，三格式解析逻辑）
- dev server 验证：6236fcde 多轮 doubao 真实记录 → stats 行多了「工具调用 1」+「协议 chat」徽标；对话预览完整。

### code review（sub-agent audit）+ 修复
- 交 code-reviewer sub-agent 全量审查对话预览功能，产出 1 P0 + 3 P1 + 6 P2。
- **P0 已修**：parseSSE.ts responses 格式 `itemToIndex` 键错——`output_item.added`/`output_item.done` 的 id 在 `item.id`（顶层无 item_id，仅 `function_call_arguments.delta` 顶层有），原代码用 `ev.item_id` 恒为 `''`，多工具流参数串参/丢失。改为 added/done 取 `item.id`，delta 保持顶层 item_id。
- **P1-1 已修**：reasoning `.done` 事件带全文（summary_text/text），原代码对 delta+done 双发会重复累积到 reasoningAccum。改为 done 只落 fields（与 output_text.done 语义对齐），delta 才累加。
- **P1-2 已修**：StreamMarkdownBlock 用 `marked.setOptions` + `marked.use` 污染全局单例（影响 chat/TextBlock.vue 的 breaks 与自定义 code 渲染）。改用 `new Marked({gfm, breaks})` 实例隔离 + `md.use({renderer})`。
- **P2 已修**：ToolCallAggregator.addFragment 中间态恰好合法、之后追加非法时 argumentsParsed 保持陈旧对象——改为每次重新 parse，失败即置 null。
- **P1-3 未修（用户自己的 Go 改动，不擅动）**：request-log/service.go healStuck 反查 route_attempts 的条件 `attemptErr != "" || attemptStatus != 0` 不含 result='failed' 校验——成功 attempt（status_code=200）但 [DONE] 未落地时会被误标 failed。建议 `WHERE request_log_id=? AND result='failed' ORDER BY started_at DESC LIMIT 1`。
- **P2 未修**（低风险）：charOffset 用 trim 后 line.length 偏差；多 choice 只拼 [0]（有注释）；useCollapse 全局 state 只增不减；ChatView blockKey Math.random() 兜底；30MB body split 两次峰值内存；Go route_attempts 无 request_log_id 索引 healStuck 全表扫；markdown ALLOWED_ATTR 含 id 多消息标题重复。
- 回归验证：responses 多工具流（fc_1 search + fc_2 read 参数各归其位）、reasoning delta+done 去重（"思考过程" 不重复）、arguments 陈旧 parse 归 null——三用例全 PASS；vue-tsc 全过；clean build 通过。

### request-log per-attempt 独立日志 + 失败收尾修复（本会话）
- **背景**：用户配 hy3 记 request_log，但 volcengine_auto 聚合内部切到 hy3 不记日志。根因 1：request-log/service.go HandleBeforeAttempt 用 `__virtual_model` 覆盖真实模型做能力路由匹配 → 物理模型 hy3 永远匹配不上。修：直接用 pipe.Request.Model（与 sensitive-filter 对齐），删虚拟覆盖。
- **per-attempt 改造（plan: docs/plans/2026-08-23-request-log-per-attempt.md）**：request_logs 从「一条 pipe 一条」改「每次渠道尝试一条」。migration v25 = route_attempts.request_log_id；contracts.RouteAttempt + RequestLogID；model-gateway 加 MetadataRequestLogAttemptID 常量 + proxyAttemptLog/proxyStreamAttempt 读 metadata 传 RequestLogID；request-log 删 recordedKey 早退、每次 newRequestLogID 覆写 metadataKey（收尾事件反查）与 attemptMetadataKey（attempt 行关联）；前端 RouteLogTable 内层 li 加「日志」router-link（v-if request_log_id）。
- **失败 attempt 收不到尾（用户实测发现）**：ProxyUpstreamFailed 只在 tryProxyAggregateFailover（聚合全败）emit → 普通模型失败/聚合中间失败 attempt 收不到 → 半条永久 running。修：新增 `proxy:attempt-failed` 纯通知事件，proxyAttemptLog 每次 failed 分支 Emit（不污染 failover），request-log 订阅复用 HandleUpstreamFailed。
- **self-heal 反查错表（P0）**：healStuck 只查 route_requests（外层 success 时为空）→ 失败 attempt 误标 stream_interrupted。修：按 request_log_id 反查 route_attempts（**必须 AND result='failed'**，否则流式断开 success+200 被误标 failed）；fallback route_requests。
- **code review（code-reviewer sub-agent）产出 P0×1 + P1×3 + P2×6，已修**：
  - P0 healStuck 反查加 result='failed' 限定（见上）
  - P1-1 未命中路由残留 UUID 串号 → delete 两 key + skippedKey 哨兵（pipeRequestLogID 见哨兵返回空，阻断按 request_id 反查旧行）
  - P1-2 finishRequestLog 非幂等（聚合末次 attempt 双事件双收尾）→ UPDATE WHERE 加 finished_at IS NULL
  - P1-3 proxy.go:448 after-hook 拒绝路径补 RequestLogID
  - P2 未修（低风险）：route_attempts.request_log_id 无索引 healStuck 全表扫；request-log 未命中时 model-gateway 生成的 pipe 级 UUID 残留 WARN；无候选渠道幽灵行；newRequestInstanceID 成死代码
- **测试**：TestHandleBeforeAttemptPerAttemptLogs / TestProxyAttemptLogCarriesRequestLogID / TestProxyAttemptLogEmitsAttemptFailed / TestHealStuckFetchesAttemptErrorBody / TestHealStuckSuccessAttemptNotMislabeled / TestBeforeAttemptMissClearsAttemptKeys。全绿，bin/loadout.exe 重建重启。
- **已知边界**：视觉子段（1.1/1.2）不经 gateway 管线无独立日志（vision_v2 http.Client 直连上游）；客户端断开无 [DONE] 靠 self-heal 标 stream_interrupted。

### 非流式响应可视化（本会话，plan: docs/plans/2026-08-23-request-log-nonstream-visualization.md）
- 需求：详情页「响应可视化」折叠项原只有 `log.stream===true` 才显示（三道 gate），非流式请求完全不渲染。用户指出「同一个逻辑，只是提取消息方式不同」，要求双模式支持；并拍板失败/被拦截请求也要显示（请求区 messages 对话历史有可视化价值）。
- 实现（3 commit：d99f17e / bac898b / 97d22d5，零新依赖）：
  - `parseSSE.ts` 新增 `parseNonStreamBody(body)`：按结构探测三格式（output→responses；choices→chat；content[]/stop_reason→claude），合成 1 条 parsed chunk（rawJsonString=原文保真），输出与 parseStreamBody 同构。
  - `ChatPreviewPanel.vue`：`parsed` 按 `props.stream` 分流；`showPanel` 从 `stream===true` 改为 `hasRequest || hasResponse`；非流式时间轴标题「完整响应 · 展开看原文」。
  - `RequestLogDetailView.vue`：折叠项恒渲染（内容判定下沉 Panel）；标题「流式响应可视化」→「响应可视化」；`streamView.visible` 死代码移除。
- 执行中发现并修复：探测条件 `typeof obj.type === 'string'` 会把 `{"type":"error"}` 误判 claude（node 脚本实测 FAIL 后收紧为 content[]/stop_reason）；plan 里 claude 分支丢 `const input = block.input` 声明。
- 验证：node strip-types 脚本 31 断言全过（临时脚本已删）；vue-tsc 通过（唯一报错 UserMessage.vue TS6133 是并行 session 既有改动，未动）；Chrome MCP 实测 chat/claude/responses 非流式 + 失败请求 + 流式回归（1710 chunks）五场景全过；4 条 ns* 验证数据已从 request-log.db 清理。
- 已知展示瑕疵（P2，未修）：非流式失败请求统计行状态显示「未结束」（parsed.isDone=false，实际请求已结束）；claude 非流式 usage 显示 —（formatUsage 不认 input_tokens/output_tokens，与流式一致）。

### 转发日志渠道筛选改「渠道名」粒度（本会话）
- 需求：RouteLogFilters.vue 渠道下拉显示的是 key（key1/13453114997），要改成渠道名下拉；并检查后端。
- 根因：后端 `/api/channels` 每条记录 = 一个 Key，前端 Select 用 `channel.name`（Key 名）展示。真正的渠道名在 `channel.channel_name`（同 base_url 一组共享）。
- 改动 8 文件（96+/9-）：
  - 后端：`contracts.RouteLogFilter` 加 `ChannelName`；`route-log/service.go listWhere` 加渠道名过滤（匹配 `r.final_channel_name` + 任一 attempt 的 `channel_name` 快照）；`admin-api handleRouteLogsList` 读 `?channel_name=`。
  - 前端：`useChannels.ts` 加 `groupChannelNames()`（按 channel_name 去重，兜底 name/base_url）；`RouteLogFilters.vue` Select 的 value/展示改 channel_name；`RouteLogsView.vue` 传归组后的 `channelOptions` 给筛选表单（表格展示仍传原始 channels）；`useRouteLogs.ts` `channel_id` → `channel_name`。
  - `RouteLogTable.vue` 不用改（ModelChannelRef 本来就按 base_url 分组显示渠道名）。
- 测试：新增 `TestListFiltersByChannelName`（final 快照 + attempt 快照两条路径 + 不存在 0 条）；go build + route-log/admin-api 测试全绿；vue-tsc 全过（唯一报错 UserMessage.vue TS6133 是存量/并行会话问题）；vite build 通过。
- **环境坑**：`npx vite build` 会卡 12 分钟+（npx 联网检查超时），直接 `./node_modules/.bin/vite build` 4s 完成。以后前端 build 一律绕过 npx。
- 已知限制（标记未修）：渠道改名后旧日志保留旧名快照，按新名过滤会漏旧记录（快照语义，不做历史名重建）。

### code review 修复（渠道筛选改动，code-reviewer sub-agent 审查）
- review 结论：无 P0；P1×2 + P2×6 + P3×3。已修 P1×2 + P2×1 + 补测试：
  - **P1-1 死选项**：`groupChannelNames` 原兜底链 `channel_name || name || base_url`，空渠道名会显示 Key 名，但日志快照落的是 `ch.ChannelName`（空则 ''）→ 选了匹配 0 条。修：只收 channel_name 非空渠道，空名不下拉。
  - **P1-2 脏选中态**：渠道删除/改名后 `form.channel_name` 残留旧名，Select 显示「所有渠道」但实际仍被旧筛选过滤。修：`RouteLogsView.apply()` 加 channel_name 存在性守卫，不在 channelOptions 里则回退 undefined。
  - **P2-1 测试缺口**：新增 `TestListFiltersByChannelNameMixedAttempts`（failover A→B 双渠道名各命中同一行 Total=1 无重复计数 + ChannelName+ChannelID AND 组合）。注意组合断言要选「final 指向的渠道名 + 其 key」（failover 场景下豆包+key-b1 本来就该命中，不是不匹配）。
  - **P2-3 顺手修了预存 bug**：`model-gateway/proxy.go:606` 全渠道失败汇总路径 `lastChannelName = ch.Name`（Key 名）→ 改 `ch.ChannelName`（渠道名），与 attempt 快照口径统一（原注释说「与 attempt 保持一致」但代码没做到）。同步更新 proxy_test.go 断言 `"last_channel_name":"err-key"` → `"workbuddy"`。
- 未修（记录）：P2-2 滚动升级兼容（新前端+旧后端筛选静默失效）；P2-4 query 无 trim（保守正确）；P2-5 已删/改名渠道旧日志无法从下拉过滤（选项只来自 channels 配置，日志侧 distinct 方案改动大，暂缓）；P2-6 ModelTestView 兜底口径 `channel_name || name` 与网关不一致；P3 `__all__` 哨兵冲突 / 空 watcher 死代码 / Select value=label 超长名。
- 验证：go build 全过；route-log/admin-api/model-gateway 三包测试全绿；vue-tsc（仅存量 UserMessage 报错）+ vite build 通过。

### vision_v2 原生透传失效 + 视觉识别缺独立日志（根因调查，plan 已落 docs/plans/2026-08-23-vision-v2-route-context-fix.md）
- 现象（loadout.db 真值确认，非配置问题）：用户配了 workbuddy 渠道 vision 原生透传（pos=0, models=["*"], channel_base_urls=["https://copilot.tencent.com/v2"]），hy3@workbuddy 请求仍走了视觉附加代理（step=4.1 视觉识别 doubao-seed-2-0-mini-260428@volcengine）。
- **Bug1 根因**：vision_v2 的 HandleProxyBeforeUpstream 注册在 ProxyBeforeUpstream 钩子（model-gateway/proxy.go:119 触发），早于 proxyAttempt 写 __current_channel/__current_channel_base_url（proxy.go:289/292）。此时 ChannelScopeFromMetadata 解析出的 scope 全空 → MatchChannelScopeEx 对 channel_base_urls 非空路由返回 false（透传永不命中），对全空路由返回 true（附加代理永远兜底命中）。
- **Bug2 根因**：vision_v2/describe.go callVision 直接 http.Client.Do 调视觉模型，绕过 model-gateway 主链路，无 routeLog.Start → route_requests 表只有 1 条主请求，视觉识别只有同 request_id 下的子 attempt（4.1）。
- 修复方向（已与用户确认 A+C）：A) types.ChannelScopeFromMetadata 加 __channel_hint 兜底（hint 在 BeforeUpstream 之前写入），resolveBaseURL 闭包签名 (string)string → (string)[]string，vision/sensitive/field/request-log 四调用方同步改；C) callVision 前后 routeLog.Start/Finish，视觉识别生成独立 request_id，UI 嵌套结构改 top-level。
- 待办：写 failing test（types/vision_v2）→ 改 ChannelScopeFromMetadata + callVision → 真机验证。

### vision_v2 修复实施完成（P0 透传 + P1 独立日志）
- P0 已修：`types.ChannelScopeFromMetadata` 支持 `__channel_hint` 兜底（resolveBaseURL 签名 (string)string → (string)[]string，按 key id 精确匹配或 ChannelName 组匹配）；vision/vision_v2/sensitive-filter/field-filter/request-log 五插件同步改。field-filter 保留 store JSON 兜底（toTypesChannels 转换）。测试：TestChannelScopeFromMetadataHintFallback / HintNoOverride / CandidatesDedup + vision_v2 TestWorkbuddyNativePassthroughWithHint / TestWorkbuddyHintNoMatchOtherChannel。
- P1 已修：`callVision` 每次上游调用 Start/Attempt/Finish 三连写独立 request（crypto/rand 16B → 32hex id），非流式解析 usage tokens；失败路径也收 failed。主请求下 4.1 子步骤保留（双写不冲突，mock 断言按 request_id 过滤）。
- 验证：相关 11 包测试全绿；go build bin/loadout.exe OK。待用户重启服务真机验证 hy3@workbuddy 透传生效 + 日志 2 条。
- 注意：v1 无前缀请求入口阶段渠道未知，渠道级约束仍匹配不到（设计边界，用户已确认默认不动）。

### 方向校正：视觉识别不放独立 request，挂在主请求折叠下作为 4.1 子步骤
- 用户截图明确反馈：视觉识别日志应该挂主请求折叠之下（4.1），不要列表里多出来一条独立 request。我之前理解错，把"补全 2 个请求日志"理解成"主请求 + 视觉识别各一条独立"。
- 实际：
  - 当前生产已经是 4.1 子步骤（executeToolLoop 的 visionAttempt 写到主请求折叠下），这个状态用户满意。
  - 我"补全"加的 callVision Start/Attempt/Finish 独立 request 是错方向——给回滚。
- 回滚内容：callVision 回到原始实现（不写 routeLog）；拆走的 callVisionWithMeta / newVisionRequestID / visionAttemptMeta 删掉；describe_test.go 两个独立 request 测试删掉；tool_loop_test.go mockVisionRouteLog 回滚（不记 starts/finishes）、TestVisionAttemptStepSequence 断言不用按 req-id 过滤。
- 仍保留的副修复：channelsForModel 补 ChannelName（P2-5）；ChannelsForModel request 闭包 byName 过滤 ManualEnabled（P2-7）；CandidatesDedup 测试改成真测试（P2-3）。
- review 原 P1-1（独立 request metadata 缺失）、P2-6（WithoutCancel）、P2-8（cached_tokens 解析）随 callVision 一起回滚——不再写独立 request 就没这些事。
- 最终：只动 P0 一处（types hint 兜底 + 5 插件签名）+ 三个小细节修。TODO 待真机验证 hy3@workbuddy 透传生效、视觉识别 4.1 子步骤未变。

### 根因二次确认：hint 兜底方向错了，真正修复是挪 hook 到 ProxyBeforeAttempt
- 诊断日志（routes.go 加 [debug]）证实：用户请求 model=hy3 是**裸模型名**（非 workbuddy/hy3 前缀），经模型路由落到 workbuddy 渠道组（4 key failover）。入口阶段 scope_ids=[] scope_base_urls=[] 全空 → pos=0 native 渠道级约束匹配不上，pos=4 proxy 全渠道兜底命中。
- 结论：之前 h-channel_hint 兜底方案只覆盖 v2 前缀（model 带 workbuddy/ 前缀）场景，不覆盖用户实际用法（裸名 + 模型路由）。方向错了。
- 真正修复：vision_v2 注册从 `ProxyBeforeUpstream` 改为 `ProxyBeforeAttempt`（每次渠道尝试前，__current_channel/__current_channel_base_url 已写入）——与 sensitive/field/request-log 对齐，正是用户说的"渠道+模型确定后做路由能力"。
- 测试更新：TestWorkbuddyNativePassthroughWithHint 改为模拟 BeforeAttempt（__current_channel + __current_channel_base_url 已写）；新增 TestWorkbuddyPassthroughWithHintV2Prefix（hint 前缀变体，保留兜底路径覆盖）。
- 已知限制（plan 记录）：混合渠道 failover（先 proxy 改 body 后 native 透传）时 body 已改，native 收到占位符——用户场景（全 workbuddy key）不触发，罕见场景退化非错误。
- 状态：测试全绿；bin/loadout.exe 已重建（18:11，含 [debug] 日志）。待用户重启验证：hy3 裸名 + 图 → 透传生效（不再出现 4.1 视觉识别）。验证通过后移除 [debug] 日志再重建。

### 删 error 路由 + 统一路由选择函数（用户决策："不支持就不管他"）
- types 包：删 `RouteError` 常量；新增 `SelectCapabilityRoutes(routes, capability, model, scope) []*CapabilityRoute`——非 proxy（native/历史 error 数据）短路优先（豁免优先，不依赖 position），proxy 全收集（叠加）。历史 route="error" 数据运行时自动按 native（透传）降级，零迁移。
- 五个调用方统一：vision_v2 / sensitive-filter / field-filter / request-log / model-gateway.RouteCapability 全部改调 SelectCapabilityRoutes；handler 判断从 `Route == RouteNative` 改 `Route != RouteProxy`（历史 error 数据落入透传分支）。
- sensitive-filter 删"命中拒绝"（error 路由）——命中敏感词不再 400，只能替换或透传（内容安全行为变更，用户已确认）。
- 前端：CapabilityRouteEditor.vue（sensitive_filter routeOptions 删 error + submit 校验删）、CapabilityRouteTable.vue（routeLabel/routeVariant 删 error）、lib/types.ts（route 类型删 'error'）。
- 测试：types 新增 TestSelectCapabilityRoutes；sensitive-filter error 测试改为"历史 error 数据降级透传"语义（TestErrorDataDegradedPassthrough 等）；vision TestHandleBeforeUpstreamError 改降级断言；model-gateway TestRouteCapability 测试数据修正（deepseek-* 改 deepseek-v4-* 避免短路误伤精确 proxy）。
- 验证：plugins 全部测试绿；vue-tsc 过；vite build 过（NODE_OPTIONS="--use-system-ca" 绕 safe-delete）；bin/loadout.exe 重建（18:31）。

### code review 修复（删 error + 统一选择函数，code-reviewer 审查）
- 结论：可合并，无 P0；P1×1 + P2×4。
- P1 已修：failover「proxy→native」body 污染——rewrite.go 非 proxy 分支清除 __vision_v2_active/__vision_v2_format/__vision_v2_route 标记（防 StreamChunk/AfterUpstream 误拦截 native 透传流）；body 占位符无法还原，plan 已记录已知限制。
- P2 已修：sensitive-filter 残留 error 注释删 + containsAny 死代码删；SelectCapabilityRoutes 注释补"历史 error 短路屏蔽同 model+channel proxy"；Editor submit 时 route==='error' 归一化 native（防写回废弃值）；rewrite_test v2 前缀变体注明是防御兜底路径（生产 BeforeAttempt 恒有 __current_channel）。
- 验证：build OK；types/vision/vision_v2/sensitive/field/request-log/model-gateway 全绿；vue-tsc + vite build 过；bin/loadout.exe 重建。
