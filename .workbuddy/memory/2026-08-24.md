## 2026-08-24

### 发布 v0.7.0 + refs/remotes 写入拦截坑（重要）
- 发布 v0.7.0（桌面端系统托盘 + MCP 生命周期 + SSO + SPA fallback + docs 归档），commit e4b2452，已推远程。
- **环境坑（必记）**：本机（WorkBuddy 环境）对 `.git/refs/remotes/` 下 **git 自身的写入**（lock+rename 机制）有系统级拦截：
  - 现象：`git fetch` 报成功（`* [new branch] master -> origin/master`）、reflog 正常写入，但 `refs/remotes/origin/` 目录整体消失 → `git status` 显示 `origin/master [gone]`，`git rev-parse origin/master` 失败。
  - 已排除：非磁盘问题（shell 直写存活）、非 git 版本（2.55/2.53 都坏）、非 fscache/appendatomically、非 ACL、非 reftable。`refs/heads/`、`refs/tags/`、`refs/notes/`、`refs/backup/` 写入全部正常，**唯独 refs/remotes/**。
  - workaround：shell 直写 tracking ref 有效。脚本 `scripts/git-sync-remotes.sh`：`git fetch` + `ls-remote` 重建 refs/remotes/origin/*。以后 fetch 一律用它（或 fetch 后手动补 ref）。
  - 未定位到拦截者（schtasks/tasklist 被 WorkBuddy 安全策略禁）。怀疑 WorkBuddy 环境或安全软件对 git 写 refs/remotes 的 rename 有精确规则。
- 另发现 `.git/refs/backup/main`（= 0afe8c5，历史 commit）——某工具/操作在 08-23 22:18 留下，来历不明，不影响使用。

### UnifyAI 模型来源卡片接真实数据（去 mock）
- 背景：`frontend/src/lib/unifyai.ts` 的 `fetchModelSource()` 原来是纯 mock（`{kind:'proxy', url:'http://localhost:10100', count:372}`），刷新元数据按钮也是假实现（`updateMetadata()` 只 `console.warn`）。
- 真实链路：unifyai CLI `--update-metadata` 拉 `https://openrouter.ai/api/v1/models`（公开端点、无需 key）→ 缓存 `~/.unifyai/cache/openrouter-models.json`（格式 `{id,name,context,output,vision,reasoning}`，本机 422 个模型）。
- 后端：`plugins/unifyai/service.go` 加 `ModelSource()`（读缓存文件 + mtime + vision/reasoning 统计，不调 CLI，离线可用）+ `OpenRouterMeta`/`ModelSourceStatus` 结构 + `maskSecret`；admin-api 注册 `GET /api/unifyai/model-source`。
- 前端：`ModelSourceStatus` 改为 `{kind:'openrouter'|'none', baseUrl, apiKeyMasked, modelCount, visionCount, reasoningCount, cachedAt, degraded}`，`fetchModelSource()` 调真实接口失败回落 `INITIAL_MODEL_SOURCE`；`CommandPreview.vue` 卡片展示 OpenRouter 配置（模型数/视觉/思考 badge + 缓存时间，`metadataUpdatedAt` prop 已删，改从 `modelSource.cachedAt` 派生）；刷新按钮 `handleUpdateMetadata` 改调 `POST /api/unifyai/run {args:['--update-metadata']}` 后轮询 model-source（≤20s）。
- 测试：`go test ./plugins/unifyai/ ./plugins/admin-api/` 全过；`vue-tsc --noEmit` + `pnpm build` 通过。**注意：改完 Go 后端要重新 `go build`（或重启服务）接口才生效。**
- 追加：`buildCommand` 命令预览开头从裸 `unifyai` 改为 `npx unifyai@latest`（与后端 `resolveCmd` 的 `npx -y unifyai@latest` 执行方式对齐，前端预览省略 `-y`）。

### 数据预览加「OpenCodex 模型」卡片 + unifyai CLI `--list-models` 参数
- **unifyai CLI**（D:\Code\Git\unifyai）：`config-loader.mjs` 把 load() 里「从 OpenCodex 代理获取模型」逻辑抽成 `fetchOpenCodexModels(configPath, {quiet})`（读 config.json → GET localhost:{port}/v1/models 带 x-opencodex-api-key 3s 超时 → 过滤 disabled provider / disabledModels，返回结构化结果含 degraded）；`cli.mjs` 加 `--list-models` 参数（人类可读 + `--json` 两种输出，JSON 模式静音 console 保证 stdout 纯净）。
- **OpenRouter 元数据匹配**：`--list-models` 输出前调 `MetadataFetcher.enrich(models, orModels)`（读 `~/.unifyai/cache/openrouter-models.json` 422 条缓存，`findInOpenRouter` 模糊匹配：完整 ID → 裸名末尾 → 标准化 → `-ga-<日期>` 剥离 → 版本后缀剥离 → name 包含），模型带出 contextWindow / supportsVision / supportsThinking；JSON 输出新增 `orMatchedCount`/`orTotal`，人类输出打印命中率与 🧠/👁️ 图标。本机实测 25 个模型命中 16 个。
- **loadout 后端**：`plugins/unifyai/service.go` 加 `OpenCodexModels()`（调 `npx -y unifyai@latest --list-models --json` 解析）+ `OpenCodexModel`/`OpenCodexModelsResult` 结构（含 ORMatchedCount/ORTotal/supportsVision/supportsThinking）；admin-api 注册 `GET /api/unifyai/opencodex-models`。
- **前端**：`unifyai.ts` 加 `OpenCodexModelsResult`/`OpenCodexModel`/`OpenCodexGroup` 类型 + `fetchOpenCodexModels()`（失败回落 `INITIAL_OPENCODEX_MODELS`）+ `groupOpenCodexModels()` 分组；`CommandPreview.vue` 加第三张卡片（模型数 / provider 数 / OpenRouter 匹配徽标 / API Key 预览 / 可展开按 provider 分组模型列表，模型行带 🧠/👁️ 能力图标，`sm:col-span-2` 占整行）；`UnifyaiPanel.vue` onMounted 拉取并传参。
- 本机实测：CLI 返回 26 个模型、过滤后 25 个（仅 Loadout provider 启用），OpenRouter 命中 16/25。`go test` 全过、`vue-tsc` + `pnpm build` 通过。
- 追加：`--list-models` 支持 `--enable-vision` 透传（后端 `OpenCodexModels(enableVision bool)` 追加 CLI 参数，admin-api `GET /api/unifyai/opencodex-models?enableVision=1`，前端 `fetchOpenCodexModels(enableVision)` + OpenCodex 卡片加「强制视觉」Switch 开关，切换后重新拉取）。本机实测开启后 25/25 全部 vision=true。

### UnifyAI 导入对话框支持「分组 / 聚合」端点（三种 kind）
- 背景：`UnifyaiPanel.vue:1062-1105` 的「从 MCP 管理导入」对话框原来只拉 `/api/mcp-servers`（单 MCP），MCP 管理页（`McpPanel.vue`）的「连接端点配置」tab 已有 单MCP/分组/聚合 三种 kind（`useMcpManagement.ts` 的 `endpoints` computed 合并三源）。
- 数据层：`frontend/src/lib/unifyai.ts` 新增 `McpImportKind`（'单 MCP'|'分组'|'聚合'）+ `McpImportSource`（name/kind/path/count/server: McpServerInfo）；`fetchManagedMcpServers()` 重写为并行 `Promise.all` 拉 `/api/mcp-servers` + `/api/groups` + `/api/mcp-tools`（聚合工具数），三接口任一失败 catch 兜底 null 不阻塞；分组/聚合的 `server` 统一为 `remote` 类型，URL 用 `window.location.origin + /mcp/<encodeURIComponent(name)>`（分组）/ `/mcp/$mart`（聚合）；新增 `importKindBadgeClass(kind)` 返回 tint 配色（单MCP=slate、分组=violet、聚合=amber，`bg-{c}-500/15 text-{c}-700 dark:text-{c}-300 border-{c}-500/20`）。
- 组件：`UnifyaiPanel.vue` importSource ref 类型改 `McpImportSource[]`；模板每行显示 server.name + path + kind Badge（tint）；`importSelectedServers` 用 `item.server` 落 mcp.json（去重判断用 `server.name`）。
- 验证：`vue-tsc -b --force` + `vite build` 通过（859 modules）。注意 `pnpm build` 第一次报 CommandPreview 缺 `opencodexModels` 是 vue-tsc -b 增量缓存假错，`--force` 重跑即过。
- 关键文件：`frontend/src/lib/unifyai.ts`、`frontend/src/components/unifyai/UnifyaiPanel.vue`。

### 聚合端点的 mcp.json key 去 `$`（$smart → mcp-smart）
- 背景：聚合端点固定是 loadout server 的 `/mcp/$smart`，但 mcp.json 的 key 带 `$` 在 OpenCode/Codex/Claude 等 TOML/JSON 配置里不友好。
- 修改：`unifyai.ts` 的 `fetchManagedMcpServers()` 聚合分支里，`name` 和 `server.name` 从 `$smart` 改为 `mcp-smart`（UI 勾选 key + mcp.json key 一致）；`path`/`url` 仍是 `/mcp/$smart` 不变，指向原聚合端点。
- 验证：`vue-tsc -b --force` + `vite build` 全过。顺带确认 UnifyaiPanel.vue:1001 的 `enableVision = v`（外部已修，之前 vue-tsc 报 999 行 `.value` 是文件在我检查后又被外部改过的时序问题）。

### 「强制视觉」Switch 接通 CLI 命令预览与实际执行
- 背景：用户在数据预览卡片里发现「强制视觉」Switch 切来切去**命令预览里的 `--enable-vision` 不变**——这是个隐蔽 bug：之前 `unifyai.ts` 的 `buildArgs()` 写死 `args.push('--enable-vision')`，`enableVision` ref 只用来控制 OpenCodex 模型拉取，没进 `commandOpts`，所以命令预览 + 实际 stream URL 都不响应 switch。
- 修改：`buildArgs()` 加可选 `enableVision?: boolean`（默认 true 保留旧版「始终追加」语义），`--enable-vision` 改 `if (opts.enableVision ?? true)`；`UnifyaiPanel.vue` 的 `commandOpts` 加 `enableVision: enableVision.value`，让命令预览（`buildCommand(commandOpts.value)`）与 SSE 执行（`buildArgs({...commandOpts.value, dryRun})`）两路同步。
- 验证：`vue-tsc -b --force` + `vite build` 全过（`UnifyaiView-CeGuqaLa.js` 56.74 kB）。
- 关键文件：`frontend/src/lib/unifyai.ts`、`frontend/src/components/unifyai/UnifyaiPanel.vue`。

### Switch 真实 bug：API 是 modelValue 不是 checked
- 背景：「强制视觉」Switch 切了视觉变、但命令预览里 `--enable-vision` 始终不出现。
- 真因不在 ref 赋值、不在 buildArgs、不在 HMR——而是 **shadcn-vue/reka-ui `<Switch>` 的 prop/emits 是 `:model-value` + `@update:model-value`，不是 `:checked` + `@update:checked`**。
- 之前 CommandPreview.vue 写的是：
  ```html
  <Switch :checked="enableVision" @update:checked="onToggleVision" />
  ```
  - `:checked` 在 reka-ui Switch 上不存在，被忽略
  - `@update:checked` 这个事件 Switch 永远不发，**`onToggleVision` 回调从未执行**
  - 结果：Switch 内部自己管 modelValue（视觉切了），但父组件 ref 永远是 false，命令预览永远没 flag。
- 修：CommandPreview.vue 改成 `:model-value="enableVision" @update:model-value="onToggleVision"`。配合之前已改好的 `commandOpts.enableVision` + `buildArgs` 条件化，命令预览与 stream URL 现在真正响应开关。
- **坑（必记）**：本项目 shadcn-vue 用的是 shadcn-vue-cdn@0.0.4（基于 reka-ui 的 SwitchRoot），其 API 是 `modelValue` v-model 接口；用 `:checked` 写是常见误解，**视觉生效但回调不触发**。下次写 Switch 必须用 `:model-value` + `@update:model-value`（或 `v-model`）。可参考 `node_modules/.pnpm/shadcn-vue-cdn@0.0.4_*/.../switch/Switch.vue.d.ts` 里的 emits 声明。
- 验证：`vue-tsc -b --force` 通过。
- 关键文件：`frontend/src/components/unifyai/CommandPreview.vue`。

### 前端 alert/confirm 全部替换为 shadcn-vue 确认框
- 全量扫描 `frontend/src`：源码 **alert 0 处**；原生 `window.confirm` 仅 1 处（`UnifyaiPanel.vue:123` removeServer），已替换为全局 `confirmDialog`（`@/composables/useConfirm` + App.vue 挂载的 `ConfirmDialog.vue`，基于 shadcn-vue AlertDialog）。
- 验证：全局 grep 无残留（只剩 useConfirm.ts 注释），`vue-tsc -b` 通过。
- 结论：项目 confirm 已统一走 `useConfirm` composable，后续新代码直接 `await confirmDialog(...)`，禁止再写 `window.confirm`/`alert`。

### ⚠️ Git 仓库数据丢失事故（比 refs 坑严重得多，必记）
- **现象**：`git status` 报 `not a git repository`。查因：**整个 `.git/refs/` 目录被删（含 refs/heads）+ `.git/objects/pack/` 的 `.pack` 数据文件被删（只剩 .idx）+ 部分 loose objects 被删**。这是此前"refs/remotes 写入拦截"的全面升级版——同一系统级拦截，这次连本地分支引用和 pack 数据都清了。
- **损失盘点**：本地未推送的 commit `e66f47cb`（"commit: 1"，02:35）object 丢失，远端没有，**不可恢复**（内容可能等于工作区未提交的 6 个修改文件）。其余全部历史在 GitHub 远端，无损。
- **恢复流程（远端可达时，完整可复用）**：
  1. 手工重建 `mkdir -p .git/refs/{heads,remotes/origin,tags}`，用 reflog 最后条目写回 refs（`logs/refs/heads/master`、`logs/refs/remotes/origin/master` 还在）。
  2. fetch 前 git 会校验 refs/heads/master 指向的 object——若指向已丢的 commit 会报 `bad object`。**绕法**：先 `printf '<备份仓库里的合法commit>' > .git/refs/heads/master` 锚定（从 `.git.backup-*/objects/pack/` 拷 pack 进来或用其 `rev-list --all` 拿 hash）。
  3. `git fetch origin --prune --tags`（即使报 refs 更新失败，object 已下载到新 pack——20MB 那个就是）。
  4. 清掉指向不存在 pack 的 `multi-pack-index`/`.idx`（fetch 前就删）。
  5. fetch 后 refs/remotes 又被清 → 手动 `ls-remote` + `printf` 重建（同 git-sync-remotes.sh 逻辑）。
  6. `rm -f .git/index`（旧 index 引用丢失 blob 会报 `unable to read`）→ `git reset --mixed HEAD` 重建干净 index → `git reflog expire --expire=now --all` 清孤儿 → `git fsck` 验证。
- **教训**：本环境 git 数据随时可能被系统级拦截删除。**远端是唯一可靠备份，务必勤 push**；本地 commit 尽量当天推。fetch 永远用 `scripts/git-sync-remotes.sh`。
- 遗留：远端历史里混入了 `.git.backup-20260822-1704` 目录（8/22 事故时被误 commit+push 的历史污点，73 个 commit 都含它），后续建议 `git rm -r --cached` + .gitignore + 新 commit 清除。`.git.broken`、`.git.backup-*` 目录仍在磁盘，数据已用完，可删可留。
- **收尾**：`.gitignore` 加 `.git.backup-*/` + `frontend/build.out`（vite 构建日志）；`git rm -r --cached .git.backup-20260822-1704` 移除跟踪（磁盘保留）；commit `699c6c92`（含 unifyai 6 文件改进 + git-sync-remotes.sh + memory 更新）已 push 到远端。push 后 refs/remotes 照例被清，手动 `printf` 重建（注意 hash 要用 `git rev-parse HEAD` 的完整值，别手填）。
