# 2026-08-20 工作日志

## 视觉模型日志落库调研 + 计划文档

- 调研「视觉模型执行日志缺失」问题，用 CodeGraph 定位根因。
- 根因：vision 插件 `callVision` 直接 http.Client.Do 调视觉模型，不走 model-gateway 主代理链路 → 不触发 route-log；vision 插件 Manifest 未注入 route-log；现有视觉日志全是 slog 进程日志。
- 审核发现 4 个落地坑：① request_id 两套（中间件 vs model-gateway 各自生成）；② 视觉 attempt 写入时机早于 route_requests.Start；③ step_no 唯一性 + action 判断冲突；④ 前端缺「视觉识别」action 标签。
- 产出计划文档 `docs/VISION-ROUTE-LOG-PLAN.md`（待评审），含三阶段方案：request_id 统一 / 视觉日志暂存+flush 落库 / 前端 action 标签，附决策点 D1-D4。

## 视觉日志落库：实施完成（三阶段）

- 阶段一：servercore 中间件优先客户端 X-Request-Id（空则生成写回请求头）；model-gateway 兜底生成提前到 hook 前。
- 阶段二：contracts 新增 VisionAttemptLog + MetadataKeyVisionAttempt("__vision_attempt")；vision 成功/失败暂存 pipe.Metadata（Describe/callVision 返回成功渠道 id）；model-gateway 新增 flushVisionAttempt，proxyBeginLog/proxyRejectedLog 的 Start 后落库（step_no=-1、action=视觉识别）。
- 阶段三：RouteLogTable.vue ACTION_LABELS 加「视觉识别」teal 标签。
- 测试：TestHandleProxyBeforeUpstreamStoresVisionLog（vision 侧暂存）、TestHandleProxyFlushVisionAttempt / Failed（model-gateway 侧落库，成功+失败拒绝两路径）。全量 go test 通过，唯一失败 core/linkfs TestLinkAndIsLink 是 Windows symlink 权限环境问题（与改动无关）。
- 实现差异：vision 插件未注入 route-log（落库统一由 model-gateway flush，插件零新增依赖）。
- 注意：List 不返回 attempts（Detail 才加载），测试断言 attempts 须用 rl.Detail。

## Code review 结果与修复（子代理 code-reviewer）

Review 结论：无 P0，可合入。已修复 3 个 P1 + 2 个 P2：
- P1-1：补真实端到端测试 plugins/vision/proxy_vision_e2e_test.go（TestVisionE2EFlushOnSuccess / FlushOnFail，完整链路：HandleProxy + 真实 vision hook + 真实 route-log，需 mock e2eCtx(plugin.Context) + e2eHealth(ModelHealth 18 方法)）
- P1-2：post-hook request_id 兜底沿用 hook 前 id（不生成新 id 破坏一致性）
- P1-3：proxyBeginLog 的 Start 失败即 return，不 flush（与 proxyRejectedLog 行为统一）
- P2：flushVisionAttempt 改用 ok 判断（去掉 ViaModel=="" 双义）；FinishedAt 用 StartedAt+Duration 对齐
- 未修（接受）：Metadata image_count/capability 前端不可见（保留 DB 可查）；D1 的 FailureCount 未加（via_options 通常 1 个）
- 坑：渠道模型必须 Enabled:true 才被 modelgateway 匹配（vision 的 channelsForModel 不查 Enabled）；db.ChannelModel{Model: x, Enabled: true}

## 视觉历史图片去重（影子消除）：设计 → 实现 → review 完成

- 问题：多轮对话客户端每轮携带完整历史，vision 插件对历史旧图无差别处理 → 每轮重复识别（缓存 miss 时重复花钱）、流式「图片理解」前缀每轮注入（客户端存进历史形成影子）、主模型上下文被图片描述牵引。
- 方案（头脑风暴确认 A）：位置感知分级——最后一条 user 消息的图片=新图（完整识别，md5 缓存优先）；历史旧图=只读缓存描述替换 / `[图片]` 占位符（不调视觉模型、不输出流式前缀）；`VisionHistoryMode` 配置 cache/placeholder/keep。
- 实现：`core/config/config.go` VisionHistoryMode；`plugins/vision/service.go` resolveHistoryImages + visionCacheKey（单一来源，Describe 共用）；`plugins/vision/proxy.go` proxyMessageRole + proxyImageHit.role + splitProxyGroups + HandleProxyBeforeUpstream 分级；测试 history_test.go（5）+ proxy_test.go 追加（5）。
- 与并行会话（视觉日志落库，Describe 3 值 + storeVisionLog）改动共存，已核实无冲突：storeVisionLog 与 Describe 同用 len(newGroup.hits)>0 守卫；ImageCount 用新图数。
- Code review（code-reviewer 子代理）：无 Critical；修复 I-1（VisionHistoryMode 非法值读取处校验 + normalize 测试）、I-2（visionCacheKey 抽函数）；顺手修 M-1（ImageCount 口径）、M-3/M-4（补计数断言）、M-5（空行）；M-2（responses 顶层 input_image 检出缺口，预存在，登记跟进）；M-7（首轮历史图无缓存项，已文档化到 IMPLEMENTATION.md）。
- 验证：go test ./plugins/vision/ ./core/config/ ./plugins/model-gateway/ ./plugins/contracts/ 全绿。

## 发现：git 仓库损坏（未修复，用户暂不处理）

- .git 只剩 182K：objects 几乎全空、refs/heads 无文件、HEAD 指向不存在的 commit，index 里 staged 全部文件（cache-tree 全部失效）。
- 提交文档时报 "invalid object ... .github/workflows/ci.yml"；go build/test 需加 `-buildvcs=false` 跳过 VCS stamping。
- 修复预案（用户说先不管）：移走 .git 后重新 clone origin（远程 imohuan/loadout 应有完整历史），再恢复本地未提交文件（docs/plans/、问题.md、.workbuddy/memory/ 等）。曾尝试 mv 目录失败（busy），但移 .git 本身成功过又移回。

## 视觉日志：多 via_option 失败记录修复（v2）

生产反馈：doubao 404 失败换 qwen3 成功，UI 只看到 qwen3 一条「视觉识别」，doubao 失败被吞。Review 时的 P1-1/D1 在生产里命中（via_options 通常 ≥2）。

修复：MetadataKeyVisionAttempt 单条 → MetadataKeyVisionAttempts []VisionAttemptLog 切片；vision/proxy.go 失败/成功都 appendVisionLog；model-gateway/proxy.go flushVisionAttempts 按 idx 赋 step_no=-(n-idx)（先尝试 step_no 更小，ORDER BY step_no ASC 时按时间顺序在前）。

测试更新：vision TestHandleProxyBeforeUpstreamStoresVisionLog 改切片断言；TestAppendVisionLogNilMetadata 覆盖 append；model-gateway TestHandleProxyFlushVisionAttempts 模拟 doubao 失败 + qwen 成功（断言两条视觉 attempt）；TestHandleProxyFlushVisionAttemptsFailed 模拟全部失败。

lesson: route-log attempt 必须按「每真实尝试一条」设计，不能聚合。Review 时的"影响小"判断错了——生产 via_options 通常 ≥2 是常态。

## 修复 MCP 管理页空数据崩溃（Cannot read properties of null reading 'length'）

- 症状：MCP 管理页无数据时（首次使用/未配置任何 MCP），控制台报 `McpPanel.vue:323 TypeError: Cannot read properties of null (reading 'length')` + reka-ui "Deferred DOM node could not be resolved" 伴随警告 + useMcpManagement.ts:139 Vue render 错误。
- 根因：Go 空表/文件不存在时返回 **nil slice** → `json.Encoder` 编码为 JSON `null`（不是 `[]`）。`readMCPServers`/`readGroups`/`readToolStates` 的 ErrNotExist 分支 `return nil, nil`，SQLite 路径 `var x []T` 空表循环不执行也返回 nil。前端 `assignList(servers, null)` 把 ref 置 null → 模板 `.length` 炸。
- 修复（双层）：
  - 前端 `useMcpManagement.ts`：`assignList` 改为 `target.value = next ?? []`（一处兜底所有列表）。
  - 后端 `plugins/admin-api/admin_helpers.go`：`readMCPServers`/`readGroups`/`readToolStates`/`readCapabilityRoutes` 空数据统一返回空 slice（`[]types.X{}`），SQLite 分支 nil 也规范化 → JSON 输出 `[]`。
- lesson：Go 后端列表接口必须保证空数据输出 `[]` 而非 `null`；前端 `assignList` 这类批量赋值也要防御。同文件 `readCapabilityRoutes` 同类坑顺手修了。
- 验证：`go build ./plugins/admin-api/` ✓、`vue-tsc -b` ✓。

## 修复设置页 credentials tab 空数据崩溃（同类 nil length bug）

- 症状：`ManagementView.vue:177 TypeError: Cannot read properties of null (reading 'length')`（credentials tab）+ reka-ui 伴随警告。
- 根因：`handleKeysList` 返回 `{"sk_keys": ..., "mcp_keys": ...}`，`ListAPIKeys()`（gateway-keys manager，JSON 文件不存在时 nil）和 `readMCPKeys`（ErrNotExist 分支 `return nil, nil`）空数据时都是 nil slice → JSON `null`。模板 `keys?.sk_keys.length` 只保护了 keys 对象，字段级 null 照样炸。
- 修复：
  - 后端 `service.go handleKeysList`：`skKeys`/`mcpKeys` nil 统一规范化空 slice；`admin_helpers.go readMCPKeys` ErrNotExist 分支返回 `[]types.MCPKey{}`（同上次 readMCPServers 模式）。
  - 前端 `ManagementView.vue`：`keys?.sk_keys?.length`（v-if）、`keys?.sk_keys || []`（v-for）、`plugins?.plugins?.length || 0`（计数）、`plugins?.plugins?.length`（v-if）、`plugins?.plugins || []`（v-for）。
- 排查确认安全：`plugins` 数组后端初始化为空 slice；`CheckResult.Issues` 在 `core/plugin/loader.go ChecksByPlugin` 已做 nil→[] 规范化，`check.issues.length` 无需改。
- lesson：Go 返回 map 里嵌 slice 字段时，字段级 nil 同样编码为 null；前端对「对象.数组字段.length」要用 `?.` 链到数组本身。
- 验证：`go build ./plugins/admin-api/` ✓、`vue-tsc -b` ✓。

## 修复分页组件 NaN/undefined value 报错（DataPagination + shadcn-vue-cdn 语义坑）

- 症状：路由日志页（RouteLogsView）每次自动刷新（setInterval → useListLoader data.value 变化 → 重渲染）控制台刷 `Invalid prop: type check failed for prop "value". Expected Number with value NaN, got Undefined`，栈指向 PaginationListItem value=undefined。
- 根因：**shadcn-vue-cdn 包的 PaginationItem 渲染的是 PaginationListItem（reka-ui，`value: {type: Number, required: true}`）**，不是官方 shadcn 的 `<li>` 容器。`DataPagination.vue` 用官方 shadcn 惯例写了 4 个「PaginationItem 包 PaginationFirst/Previous/Next/Last」——这 4 个 PaginationItem 没传 value → 透传 undefined 给 PaginationListItem → 必填 Number 校验炸。页码项 PaginationItem 有 `:value="item.value"` 所以不报。
- 修复：`DataPagination.vue` 去掉 4 个导航按钮的 PaginationItem 包裹（PaginationFirst/Previous/Next/Last 是独立组件，直接放 PaginationContent 内，靠 PaginationRoot provide 取上下文），只给真正的页码项保留 PaginationItem（带 value）。
- lesson：本项目的 shadcn-vue-cdn（imohuan 的包）PaginationItem = PaginationListItem 语义（按钮 + value 必填），**不要按官方 shadcn-vue 文档那样用 PaginationItem 包导航按钮**。PaginationList 的 items 只有 page/ellipsis 两种（Gh/Hh/Uh，reka-ui utils），pageCount=1 时正常返回 [1]。
- 验证：`vue-tsc -b` ✓。

## 视觉日志：两阶段写入重构（v3，用户反馈日志时机）

用户反馈：识别完成才出现日志（视觉识别阻塞在主请求里，flush 在 hook 后）。要求访问时写占位、响应结束后更新状态。

重构（去掉 v2 的 Metadata 暂存+flush 机制，改为直接两阶段写）：
- route-log Start SQL UPSERT 增加 requested_model/virtual_model 更新（占位 + hook 后补全虚拟模型）
- model-gateway HandleProxy 在 before-upstream hook 前调用 proxyBeginLog 写占位 running；删除 flushVisionAttempts；proxyRejectedLog 保留幂等 Start + Finish
- vision 插件注入 route-log（SetRouteLog）；HandleProxyBeforeUpstream 识别开始写 running 占位（step_no=-(n-idx)），结束更新 success/failed
- contracts 删除 VisionAttemptLog/MetadataKeyVisionAttempts（v2 机制废弃）

测试：vision TestHandleProxyBeforeUpstreamVisionLogTwoPhase/MultiCandidate（mock routeLog 断言 running→终态序列）；model-gateway TestHandleProxyStartBeforeHook/TestHandleProxyVirtualModelSecondStart；TestHandleProxyRejectedBeforeUpstreamWritesLog 断言改为 starts=2（占位+补全）。

注意：route-log Start 现在可能调用 2 次（占位+补全），UPSERT 按 request_id 合并；Start 断言类测试需按 2 次理解。

## step_no 改为单调递增正数空间（v4，用户反馈）

用户反馈：视觉 attempt 用 -1/-2/-3 序号反直觉，应该是 1, 2, 3, 4 连续递增（"第一步应该就是一啊"）。

修复：视觉 step_no = idx+1（正数，与主链路共享单调递增空间）；视觉循环结束设 pipe.Metadata["__route_step"]=visionStep；主链路 proxyAttemptLog 从 __route_step +1 续接；action 判断改用独立的 __main_route_step 计数器（避免视觉 step 占用导致首次尝试被误判为「切换渠道」）。

bug：成功路径 return pipe, nil 在循环内，跳过了循环外的 __route_step 写入，导致主链路从 1 起步与视觉 step=1 冲突。修复：成功 return 前也写入。

测试断言全部改正数：vision 单候选 step=1、多候选 1,2；E2E 单候选视觉 step=1 主链路 step=2。

## 前端详情重试（v4）

RouteLogsView.refreshActiveDetails 之前只刷 running，终态不再刷详情，导致详情首次延迟/失败时 UI 长期不完整。新增 shouldRefreshDetail：终态 + attempts 空 → 重试最多 5 次（detailRetryCount Map 计数）；拉取成功且 attempts 非空则清零。

## v4 review 修复

code-reviewer 结论：需返工（无 P0）。已修：
- P1-1 纯历史旧图 step 空悬：visionLogging 提到循环前，visionStep/__route_step 只在 visionLogging 时更新；纯旧图不占 step 空间（主链路从 1 开始）
- P1-2 前端无限重试：refreshActiveDetails 成功后终态 attempts 仍空也计数；达 5 次 expandedIds.delete 收起；注意 for 内用 continue 不是 return（return 会退出整个函数）
- P1-3 测试：新增 TestHandleProxyBeforeUpstreamPureHistoryNoStepPollution（纯旧图不写 attempt、不占 step）
- P2 expand() 补 catch（首次失败由定时刷新兜底）
- 未补（记录缺口）：视觉全失败+aggregate skipped 续接、aggregate 递归 failover 跨轮次 step——因场景组合罕见，且已由 TestHandleProxyRejectedBeforeUpstreamWritesLog 覆盖 aggregate 侧

## 渠道列表 loading 粒度：全局 → 按钮级

用户反馈：渠道页点任一按钮，整表按钮全部 disabled（原来 ChannelsView 单 useAsyncTask 共用一个 pending，ChannelTable 每个按钮 :disabled="pending"）。

后台并发调研结论（决定粒度上限）：
- `core/db/db.go`：SetMaxOpenConns(1) + WAL + busy_timeout=5000 → 并发写自动排队串行，不坏数据、不丢写，**支持按钮级并发**。
- 各操作写不同数据域（PATCH manual_enabled vs POST refresh-models 写 channel_models vs reorder 整表），跨 key 无共享状态。
- 唯一竞态：同一 key「刷新模型」×「编辑保存」都整写 channel_models，最后写者赢；刷新探测默认全 Enabled 会覆盖刚保存的禁用态。处理：编辑器保存用全局 key `save`（保存期间表格操作仍可用，但编辑器自己锁）。

实现：
- `useAsyncTask.ts`：单布尔 pending → `Set<string>` pendingKeys + `pending`(computed size>0，兼容旧调用方) + `isPending(key)`；`run` 重载 `run(task, success?)`（走 `__global__` key，向后兼容 9 个旧调用方）和 `run(key, task, success?)`。顺带修了旧实现并发时先完成任务提前解锁 UI 的 bug。
- `ChannelsView.vue`：groupKey(`group:{baseUrl}:{action}`)/keyKey(`key:{id}:{action}`) 两套 key；save 用 `run('save', ...)`；ChannelEditor :pending 改 isPending('save')；ChannelTable 传 `:is-pending="isPending"`。
- `ChannelTable.vue`：props pending? 改 isPending?；每个按钮 :disabled="busy(对应key)"，图标 pending 时换 RiLoader4Line animate-spin，停用/启用按钮显示「处理中」；编辑/添加 Key 不锁（只开弹窗）。
- 验证：vue-tsc --noEmit 零错误。
- lesson：按钮级 loading 需「run 的 key」与「按钮 disabled 的 key」同一套规则，否则对不上；组 key 里 baseUrl 要用 normalizeBaseURL 保证一致。

## 模型测试请求改「旁路探针」：不再写转发日志（用户需求）

用户需求：测试页点发送不应污染转发日志；只有调 Loadout 自己导出的服务（router 正常入口）才写日志。测试请求的访问摘要随响应回带，前端「请求记录」面板直显。

实施（后端 test_proxy.go + service.go，前端 useModelTest.ts + ModelTestView.vue）：
- `handleTestChat` 5 个分支的 `logTestResult` 全部删除（函数一并删），测试请求**永不调用 routeLog.Start/Attempt/Finish**。
- 新增访问摘要回带：非流式/错误路径用响应头 `X-Test-Log`（base64 JSON）；流式成功路径在 SSE 末尾追加 `event: route_log` 事件（data=base64 JSON）。摘要结构 `testLogSummary` 与前端 RouteLog 同形（含 attempts[0]，failure_class 只在 attempt 层）。
- 前端 `useModelTest.chat()` 第 5 参改为 options 对象（onDelta/onSummary/signal）；SSE 解析器识别 event 行 + route_log 事件；`decodeTestLogSummary` 用 atob + TextDecoder(utf-8) 解码（error_message 可能含中文）。`syncTestLog` 双源兜底：先用 summary 完整化 entry，再用 request_id 探测 detail 覆盖（上游是 Loadout 自家服务时能拉到）。
- 文案同步：ModelTestView PageHeader / service.go / test_proxy.go 注释全部去掉「测试请求会写入转发日志」。
- 测试：新增 test_proxy_summary_test.go（newRouteLogTestServer 带真实 routeLog 服务）：TestModelTestNoRouteLogWritten（非流式+流式后 route_requests 表必须 0 行）、TestModelTestStreamSummaryEvent、TestModelTestErrorPathSummaryHeader；TestModelTestProxy 增强 X-Test-Log 断言。go test ./plugins/admin-api/ 全绿。

## 模型测试旁路改造：code review + 修复

code-reviewer 审查结论：无 P0，可合入，修 2 个 P1。已修：
- P1-1 错误路径重复行：chat() throw 前 onSummary 已写入真实 request_id，但 catch 分支 entry.request_id 还是占位 test-xxx → syncTestLog 按真实 id 找不到占位行 → unshift 出两行。修：catch 用 summaryHolder 覆盖 request_id + syncTestLog 按对象引用（log === entry）兜底定位。
- P1-2 abort 无摘要丢失持久化：旧代码 catch 有 saveTestRouteLog(entry)，重构后丢了。修：syncTestLog catch 分支补 `if (!summary) saveTestRouteLog(entry)`。
- P2-3 并发加固：send() 顶部加 `if (streaming.value) return` 硬保护；testSummary 共享 ref 改为 send() 闭包内 `summaryHolder = { value: RouteLog | null }` 对象容器（对象属性不被 TS 控制流窄化）。
- P2-5 decoder flush：SSE 解析重构为 processLines() 局部函数，done 时 `buffer += decoder.decode()` flush 多字节残留后补扫。
- P2-6 rune 安全截断：test_proxy.go error_message 截断改 `[]rune` 切片（避免切在多字节字符中间）。
- P2-7 补测试：TestModelTestNoRouteLogWritten 加错误路径（上游 401）不写日志断言。
- 未修（接受）：P2-4 流式预置 header（有 running 误导风险，reviewer 也认为非必须）；P2-8 既有 300s 流式超时 / toISOString 排序小问题。
- TS 坑（最小复现确认）：**try-catch 中变量在闭包（onSummary 回调）内赋值，catch 分支读取该局部变量时 TS 保守窄化为初始值 null → 可选链取属性报 "Property does not exist on type 'never'"**。标准 TS 行为（非 vue-tsc bug）。绕过：用对象容器 `{ value: T | null }` 持值（对象属性不做控制流窄化）。
- 验证：go build ./... ✓、go test ./plugins/admin-api/ 全绿 ✓、vue-tsc -b --force --noEmit 0 错误 ✓。
- 顺手修：vue-tsc -b 增量缓存陈旧误报 useMcpManagement isPending（--force 全量重建后 0 错误，非真实问题）；useMcpManagement.ts 未改动。
- lesson：测试环境 newTestServer 没接 routeLog（s.routeLog==nil），旧测试从未真正验证过「写日志」；验证不写日志须自建带 routeLog 的测试服务器。

## 按钮级 loading 推广到全前端（模型状态/转发日志/MCP/技能/能力路由/聚合/设置）

用户反馈按钮看不到加载状态 → 根因：本地写操作毫秒级完成，spinner 一帧都画不出。`useAsyncTask.run` 加 `MIN_PENDING_MS=300` 最小展示时长（成功路径补齐，失败不补，toast 即时反馈）。

随后按同样模式推广所有页面：
- **模型状态**（ModelStatusView + List/ChannelGroup/Channel + Filters）：key 规则 `ms:{channelId}:{action}`（toggle/model-toggle:name/model-delete:name/batch-toggle:on|off/batch-recover/batch-delete/recover/model-recover:name/recover-all/check/recover-all-channels/recover-all-models/refresh/filter）
- **转发日志**（RouteLogsView + RouteLogFilters）：`refresh`/`clear`/`apply`
- **MCP 管理**（useMcpManagement + McpPanel）：`server:{id}:test|toggle|remove`、`group:{name}:remove`、`save-server`/`save-group`、端点密钥 `endpoint:{path}:create|delete`（McpPanel 独立 useAsyncTask 实例）
- **技能**（SkillsView）：`install-skill`/`import-skill`/`save-preset`/`skill:{name}:remove`/`preset:{name}:apply|remove`/`platform:{name}:restore`/`restore-all`；顶部刷新/同步/更新用各自已有状态（anyRefreshing/syncing/updateStatus）
- **能力路由**（CapabilityRoutesView + Table）：`save` + `route:{key}:remove`（key=capability|models|channel_ids，view 与 table 各复制一份同规则）
- **聚合**（AggregatesView + Table）：`save` + `aggregate:{name}:remove`
- **设置**（ManagementView）：`refresh`/`create-key`/`key:{id}:remove`/`save-settings`/`change-password`；导出/导入按钮去 pending 锁（仅开弹窗）
- 通用模式：view 用 `const { run, isPending } = useAsyncTask()`，子组件 props 改 `isPending?: (key)=>boolean` + 本地 `busy(key)` helper；图标按钮 pending 时换 RiLoader4Line animate-spin，文字按钮加 spinner + 文案切换；纯开弹窗的按钮（编辑/添加）不锁
- 遗留：LoginView/Login.vue 保持全局 pending（登录表单本来就该整体锁）；ChannelEditor/CapabilityRouteEditor/AggregateEditor 的 pending prop 由父级传 isPending('save') 等布尔值，无需改
- 验证：vue-tsc --noEmit 零错误
- 渠道编辑器加「获取模型」按钮：新增 POST /api/channels/probe（不落库）。新建传 base_url+api_key；编辑时 Key 不回显只传 id，后端按 id 取已存 Key 探测。前端 useChannels.probe() + ChannelEditor 搜索框右侧按钮，新探测模型默认启用并入候选池，失败显示 probeError。go build ./... 通过；TestChannelProbe 覆盖新建/编辑/无效id/缺base_url；vue-tsc -b --force 0 错误。
- 渠道分级（方案 B）：新增渠道名称 channel_name（Base URL 组级字段，同组 Key 冗余同步）。
  后端：types.Channel + db.Channel 加 ChannelName；db 迁移 v6 ALTER channels ADD channel_name；创建渠道显式传 channel_name，未传则继承同 base_url 已有渠道名（添加 Key 场景）或回退 Key 名；编辑任一 Key 改渠道名 → 同 base_url 全部同步（文件模式 handleChannelUpdate + DB 模式 handleChannelUpdateDB 均实现）。前端：ChannelEditor 加「渠道名称」输入框（添加 Key 模式禁用+展示 groupName，ChannelsView openAddKey 取同组首个 channel_name 传入）；ChannelTable group 行显示 channel_name 兜底 name + BaseURL；ModelTestView 预设下拉按 base_url 分组（SelectLabel 渠道名 + SelectItem Key 名）。TestChannelNameSync 覆盖创建/继承/同步/隔离。go build + go test 全绿；vue-tsc --force 0 错误。
- code review 修复（probe + channel_name）：① probe 解密失败静默吞掉→改为 500；cipher 空误判 404→区分"渠道不存在"(404) 与"渠道无 Key"(空 key 探测)；② 编辑 Key 挪组（改 base_url）时前端回传旧组名→后端强制跟随新组名（文件+DB 双模式），避免同组不一致；③ 添加 Key 模式前端不再提交 channel_name（后端继承），修复旧数据组内分叉；④ 文件模式 create 补 name/base_url 必填校验（与 DB 模式对齐）。新增 TestChannelMoveGroupName。已知限制（未修）：channel_name 空串=未修改，无法清空渠道名；api_key_cipher 密文回传列表（既有行为）；List→Replace 全量替换并发丢更新（既有架构）；probe 用 context.Background()（既有 probeChannelModels 限制）；新测试仅覆盖文件模式。
- 模型状态页 group 行加渠道名：ModelStatusChannelGroup 加 groupTitle computed（channel.channel_name 兜底 channel.name 兜底 baseUrl），模板第一行展示标题、第二行展示 Base URL（与 ChannelTable 同套路）。vue-tsc --force 通过。
- 修复模型状态页渠道名不显示：根因是数据链路缺 channel_name——contracts.ChannelStatus 无该字段 → model-health listChannelStatus SQL 未查 c.channel_name → admin-api flattenStatus 未透传。四处补齐：contracts/routing.go 加 ChannelName、model-health service.go（channelStatusRow + SQL + Scan + List 构造）、admin-api flattenStatus map 加 channel_name。go build + admin-api/model-health 测试全绿。
- 模型测试「Loadout 自带」预设：SK key 创建时用 store.Encrypt 加密明文存 APIKey.Cipher（types.APIKey 加字段；db migration v7 gateway_keys.api_key_cipher；ListAPIKeys 列表接口剥掉 Cipher 不回传）。前端传 sk_key_hash → 后端 ResolveAPIKey 按 hash 匹配记录 + Decrypt 得明文 → 配 base_url 直调自家网关（test_proxy resolveTestTarget 新增分支）。前端：预设下拉顶部加「Loadout 自带 API」（channelId='__builtin__' 哨兵），API Key 输入框在自带模式变 Select（列 sk_keys name+prefix，选 hash）；buildTarget 带 sk_key_hash。TestModelTestBuiltinSkKey 覆盖。db_test 迁移数量 5→7、未来版本 6→8。
- SK key 自带预设 code review 修复：① Critical：sk_key_hash 分支 base_url 未校验（攻击者可指向自己服务器骗明文）→ resolveTestTarget 强制 base_url host == r.Host 或本地回环（isLoopbackHost），否则 400；② 前端 presetTriggerLabel 识别 __builtin__ 显示「Loadout 自带 API」；③ skKeys 过滤 enabled！== false；④ fetchModels/send 自带模式未选 key 前置报错；⑤ 测试补恶意 base_url 400 + 删除 key 后 hash 失效 400。已知不改：ReplaceAPIKeys 静默 fallback JSON（既有行为）、APIKeyByHash 读路径不持锁（单写者低风险）、旧数据无 Cipher 需重建 key、跨机拷贝数据目录解密失败（文档级限制）。
- 自带模式 base_url 改后端自动补全：前端不再传 base_url（自带模式 Base URL 输入框隐藏 + 说明文案），后端 resolveTestTarget sk_key_hash 分支用 r.Host 构造 scheme://host/v1（TLS 判断 scheme），彻底移除用户可控 base_url（连带删掉之前加的 host 校验 isLoopbackHost）。TestModelTestBuiltinSkKey 重构为 combined server（admin + /v1/models 同 host）验证自动补全路径。
- 自带模式 base_url 双规则：前端保留 Base URL 输入框（默认 /v1 + placeholder 提示），相对路径/空 → 后端按请求 Host 补全 scheme://Host；完整 http(s):// URL → 直接使用（支持自定义域名测试）。识别自带模式仍靠 sk_key_hash（与 base_url 无关）。TestModelTestBuiltinSkKey 补相对路径 + 完整 URL 两场景。
- 请求记录 UI 优化：① 自带模式「最终目标」列显示 @ 自带 · {key名}（testLogSummary 加 SkKeyName 字段，manager.ResolveAPIKey 改返回 (plain, name, error)，resolveTestTarget 签名加 skKeyName 透传，RouteLogTable final_channel_id==='__builtin__' 时用 finalTargetLabel 渲染）；② 结果列不再走 StatusBadge（其"已关闭"覆盖逻辑会污染测试结果语义），自渲染中文 RESULT_LABELS（已成功/失败/进行中/已跳过/已中断）+ 颜色 resultTone。

## UnifyAI 配置同步页面（前端 UI，后台未接入）

按 D:\Code\Git\unifyai\docs\UI-DESIGN-SPEC.md 在 loadout frontend 实现单页工具型 UI，全链路只做前端 + 预留函数：
- 新增文件：`src/lib/unifyai.ts`（类型 + PLATFORMS 能力矩阵 + INITIAL_MCP_SERVERS mock + buildCommand CLI 拼装 + fetchModelSource/fetchMcpServers/updateMetadata/runSync 预留签名，均 console.warn 返回 mock）；`src/components/unifyai/PlatformCard.vue`（能力徽章/选中态/置灰）；`ExcludeMatrix.vue`（排除矩阵三态单元格 + 全局/按平台排除推导 + 白名单 Tab + 只显示被排除项开关）；`CommandPreview.vue`（模型/MCP 来源状态条 + 实时命令预览）；`UnifyaiPanel.vue`（同步内容三态 Tabs、平台卡片、执行前确认弹窗 §10.3、模拟执行日志流含 §8 图标语义/着色/自动滚动、平台 chips 状态、汇总卡片）；`src/views/UnifyaiView.vue`（薄包装）。
- 路由 `/unifyai`（title UnifyAI 配置同步）+ 侧边栏「配置」组新增「UnifyAI 同步」项（RiSwapLine）。
- 默认交互预置文档典型用例：node_env 对 Codex 默认排除；模式切「仅模型」时 MCP 过滤区隐藏、不支持该能力的平台卡片置灰。
- 注意：Badge variant 无 warning（用 outline）；tsconfig 开启 noUnusedLocals/erasableSyntaxOnly（禁 enum）；`buildCommand` 全局排除不重复进 per-platform 列表。
- 验证：vue-tsc 0 错误 + pnpm build 通过（UnifyaiView chunk ~30KB）。后端接入点：lib/unifyai.ts 内 TODO(backend) 函数。
