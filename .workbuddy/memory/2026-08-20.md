# 2026-08-20 工作日志

## 能力路由渠道选择：渠道级 vs Key 级严格区分（用户多轮反馈收敛）

用户 4 轮反馈，最终语义：**"选渠道"必须由点过渠道名产生；勾满整组 Key 不算选渠道**。
- ① 弹窗渠道选择 flex-wrap 按钮堆太宽 → 抽 ChannelGroupPicker（layout vertical/horizontal）
- ② 去掉"通用（全匹配）"按钮（不选即全匹配）；trigger/badge 去"（整组）"、"· N 个 Key"后缀
- ③ 支持跨渠道 Key 混选（singleChannelGroup=false 模式）；渠道级时 badges 隐藏
- ④ 显示按渠道分组聚合：`A(k1,k2), B(k3)`（formatChannelRef 跨组聚合）
- ⑤ 全勾 Key 不能冒充渠道 → 引入**渠道级多选状态 channel_base_urls[]**：渠道级（点过渠道名）显示无括号 `A`，Key 级（勾过 Key，含全勾）显示 `A(k1,k2)`；两者互斥维护（设渠道级移除该组 channel_ids；点渠道级组内 Key 取消渠道级改只选该 Key）
- 保存：selectedKeyIds = 渠道级组 keys ∪ channel_ids，展开为 channel_ids 落库（数据模型不变）；重编辑还原为 Key 多选显示（没点过渠道名就不算选渠道）
- 文件：useChannelRef.ts（ChannelGroupSegment 加 level + channelLevelSegments + mergeSegments + channel_base_urls 支持）、ChannelGroupPicker.vue（multiToggleGroup/multiToggleKey/isMultiChannelLevelSelected）、CapabilityRouteEditor.vue（channelSel.channel_base_urls + selectedGroups 合并段 + removeChannelGroup(seg)）
- ModelChannelList（单渠道模式）不受影响
- 注意：SkillsView.vue 有既存未完成改动（StreamLogPanel 未接入、logLines/ansiToHtml 缺失），vue-tsc 报错与本次无关

## 能力路由：目标渠道选择改为 ChannelGroupPicker（抽取复用 + horizontal 布局）

- 用户反馈 ①：能力路由编辑弹窗的目标渠道选择是 flex-wrap 按钮堆（newapi/Key1/volcengine...混排），太宽；② "通用（全匹配）"按钮冗余（不选即全匹配）。
- 抽取：新建 `frontend/src/components/ChannelGroupPicker.vue`——把 ModelChannelList 的渠道分组选择逻辑+UI 独立成组件，v-model 绑定 `{channel_id, channel_ids, channel_base_url}`（与 ModelChannelItem 字段兼容），支持 `layout: 'vertical' | 'horizontal'`（vertical=原组标题在上 keys 在下；horizontal=每渠道一行，左渠道名右 keys 横向排开）。抽走的函数：selectionOf/isChannelLevelSelected/applySelection/setChannelLevel/activeGroupOf/toggleKey/toggleGroup/toggleAuto/groupLabel。
- ModelChannelList.vue：内部渠道 Popover 改用 ChannelGroupPicker（vertical，行为不变）；clearModelOnChannelChange 改为 watch 行级渠道字段变化清空该行 model（原逻辑在 applySelection 里）；modelCandidates 内联展开逻辑（原依赖被删的 selectionOf/channelGroups）。
- CapabilityRouteEditor.vue：目标渠道 Popover 用 ChannelGroupPicker（horizontal、allowAuto=false）；form.channel_ids 删除 → channelSel（ChannelSelection）单一状态源；`*` 老数据归一化为空=全渠道；selectedKeyIds computed（渠道级展开组内 keys）驱动候选模型与保存；trigger 用 formatChannelRef + （整组）后缀；badges 渠道级单个+Key 逐个可删（removeChannelId/clearChannels）。
- 展示统一：CapabilityRouteTable + CapabilityRoutesView 的 channelScopeLabel 空/`*` 都显示「全渠道」（不再出现"通用（全匹配）"字样）。
- 后端无需修改：MatchChannel 对空列表/`*` 均全渠道命中；新数据只存空数组或具体 key ids；老 `*` 数据由后端兼容。
- 验证：prettier 格式化 + `vue-tsc -b --force` 0 错误。
- 坑：Vue watch 回调签名是 (newValue, oldValue)，遍历旧值对比新值找变化行；v-model 到数组元素不能用，用 `:model-value="item" @update:model-value="Object.assign(item, $event)"`（组件内部直接改传入 reactive 对象属性即可同步）。

## 视觉模型日志落库调研 + 计划文档

- 调研「视觉模型执行日志缺失」问题，用 CodeGraph 定位根因。
- 根因：vision 插件 `callVision` 直接 http.Client.Do 调视觉模型，不走 model-gateway 主代理链路 → 不触发 route-log；vision 插件 Manifest 未注入 route-log；现有视觉日志全是 slog 进程日志。
- 审核发现 4 个落地坑：① request_id 两套（中间件 vs model-gateway 各自生成）；② 视觉 attempt 写入时机早于 route_requests.Start；③ step_no 唯一性 + action 判断冲突；④ 前端缺「视觉识别」action 标签。
- 产出计划文档 `docs/VISION-ROUTE-LOG-PLAN.md`（待评审），含三阶段方案：request_id 统一 / 视觉日志暂存+flush 落库 / 前端 action 标签，附决策点 D1-D4。

## 视觉日志落库：实施完成（三阶段）

- 阶段一：servercore 中间件优先客户端 X-Request-Id（空则生成写回请求头）；model-gateway 兜底生成提前到 hook 前。
- 阶段二：contracts 新增 VisionAttemptLog + MetadataKeyVisionAttempt("__vision_attempt")；vision 成功/失败暂存 pipe.Metadata（Describe/callVision 返回成功渠道 id）；model-gateway 新增 flushVisionAttempt，proxyBeginLog/proxyRejectedLog 的 Start 后落库（step_no=-1、action=视觉识别）。
- 阶段三：RouteLogTable.vue ACTION_LABELS 加「视觉识别」teal 标签。
- 测试：TestHandleProxyBeforeUpstreamStoresVisionLog（vision 侧暂存）、TestHandleProxyFlushVisionAttempt / Failed（model-gateway 侧落库，成功+失败拒绝两路径）。全量 go test 通过，唯一失败 core/linkfs TestLinkAndIsLink 是 Windows symlink 权限环境问题（与改动无关）。
- 实现差异：vision 插件未注入 route-log（落库统一由 model-gateway flush，插件零新增依赖）。
- 注意：List 不返回 attempts（Detail 才加载），测试断言 attempts 须用 rl.Detail。

## Code review 结果与修复（子代理 code-reviewer）

Review 结论：无 P0，可合入。已修复 3 个 P1 + 2 个 P2：
- P1-1：补真实端到端测试 plugins/vision/proxy_vision_e2e_test.go（TestVisionE2EFlushOnSuccess / FlushOnFail，完整链路：HandleProxy + 真实 vision hook + 真实 route-log，需 mock e2eCtx(plugin.Context) + e2eHealth(ModelHealth 18 方法)）
- P1-2：post-hook request_id 兜底沿用 hook 前 id（不生成新 id 破坏一致性）
- P1-3：proxyBeginLog 的 Start 失败即 return，不 flush（与 proxyRejectedLog 行为统一）
- P2：flushVisionAttempt 改用 ok 判断（去掉 ViaModel=="" 双义）；FinishedAt 用 StartedAt+Duration 对齐
- 未修（接受）：Metadata image_count/capability 前端不可见（保留 DB 可查）；D1 的 FailureCount 未加（via_options 通常 1 个）
- 坑：渠道模型必须 Enabled:true 才被 modelgateway 匹配（vision 的 channelsForModel 不查 Enabled）；db.ChannelModel{Model: x, Enabled: true}

## 视觉历史图片去重（影子消除）：设计 → 实现 → review 完成

- 问题：多轮对话客户端每轮携带完整历史，vision 插件对历史旧图无差别处理 → 每轮重复识别（缓存 miss 时重复花钱）、流式「图片理解」前缀每轮注入（客户端存进历史形成影子）、主模型上下文被图片描述牵引。
- 方案（头脑风暴确认 A）：位置感知分级——最后一条 user 消息的图片=新图（完整识别，md5 缓存优先）；历史旧图=只读缓存描述替换 / `[图片]` 占位符（不调视觉模型、不输出流式前缀）；`VisionHistoryMode` 配置 cache/placeholder/keep。
- 实现：`core/config/config.go` VisionHistoryMode；`plugins/vision/service.go` resolveHistoryImages + visionCacheKey（单一来源，Describe 共用）；`plugins/vision/proxy.go` proxyMessageRole + proxyImageHit.role + splitProxyGroups + HandleProxyBeforeUpstream 分级；测试 history_test.go（5）+ proxy_test.go 追加（5）。
- 与并行会话（视觉日志落库，Describe 3 值 + storeVisionLog）改动共存，已核实无冲突：storeVisionLog 与 Describe 同用 len(newGroup.hits)>0 守卫；ImageCount 用新图数。
- Code review（code-reviewer 子代理）：无 Critical；修复 I-1（VisionHistoryMode 非法值读取处校验 + normalize 测试）、I-2（visionCacheKey 抽函数）；顺手修 M-1（ImageCount 口径）、M-3/M-4（补计数断言）、M-5（空行）；M-2（responses 顶层 input_image 检出缺口，预存在，登记跟进）；M-7（首轮历史图无缓存项，已文档化到 IMPLEMENTATION.md）。
- 验证：go test ./plugins/vision/ ./core/config/ ./plugins/model-gateway/ ./plugins/contracts/ 全绿。

## 发现：git 仓库损坏（未修复，用户暂不处理）

- .git 只剩 182K：objects 几乎全空、refs/heads 无文件、HEAD 指向不存在的 commit，index 里 staged 全部文件（cache-tree 全部失效）。
- 提交文档时报 "invalid object ... .github/workflows/ci.yml"；go build/test 需加 `-buildvcs=false` 跳过 VCS stamping。
- 修复预案（用户说先不管）：移走 .git 后重新 clone origin（远程 imohuan/loadout 应有完整历史），再恢复本地未提交文件（docs/plans/、问题.md、.workbuddy/memory/ 等）。曾尝试 mv 目录失败（busy），但移 .git 本身成功过又移回。

## 视觉日志：多 via_option 失败记录修复（v2）

生产反馈：doubao 404 失败换 qwen3 成功，UI 只看到 qwen3 一条「视觉识别」，doubao 失败被吞。Review 时的 P1-1/D1 在生产里命中（via_options 通常 ≥2）。

修复：MetadataKeyVisionAttempt 单条 → MetadataKeyVisionAttempts []VisionAttemptLog 切片；vision/proxy.go 失败/成功都 appendVisionLog；model-gateway/proxy.go flushVisionAttempts 按 idx 赋 step_no=-(n-idx)（先尝试 step_no 更小，ORDER BY step_no ASC 时按时间顺序在前）。

测试更新：vision TestHandleProxyBeforeUpstreamStoresVisionLog 改切片断言；TestAppendVisionLogNilMetadata 覆盖 append；model-gateway TestHandleProxyFlushVisionAttempts 模拟 doubao 失败 + qwen 成功（断言两条视觉 attempt）；TestHandleProxyFlushVisionAttemptsFailed 模拟全部失败。

lesson: route-log attempt 必须按「每真实尝试一条」设计，不能聚合。Review 时的"影响小"判断错了——生产 via_options 通常 ≥2 是常态。

## 修复 MCP 管理页空数据崩溃（Cannot read properties of null reading 'length'）

- 症状：MCP 管理页无数据时（首次使用/未配置任何 MCP），控制台报 `McpPanel.vue:323 TypeError: Cannot read properties of null (reading 'length')` + reka-ui "Deferred DOM node could not be resolved" 伴随警告 + useMcpManagement.ts:139 Vue render 错误。
- 根因：Go 空表/文件不存在时返回 **nil slice** → `json.Encoder` 编码为 JSON `null`（不是 `[]`）。`readMCPServers`/`readGroups`/`readToolStates` 的 ErrNotExist 分支 `return nil, nil`，SQLite 路径 `var x []T` 空表循环不执行也返回 nil。前端 `assignList(servers, null)` 把 ref 置 null → 模板 `.length` 炸。
- 修复（双层）：
  - 前端 `useMcpManagement.ts`：`assignList` 改为 `target.value = next ?? []`（一处兜底所有列表）。
  - 后端 `plugins/admin-api/admin_helpers.go`：`readMCPServers`/`readGroups`/`readToolStates`/`readCapabilityRoutes` 空数据统一返回空 slice（`[]types.X{}`），SQLite 分支 nil 也规范化 → JSON 输出 `[]`。
- lesson：Go 后端列表接口必须保证空数据输出 `[]` 而非 `null`；前端 `assignList` 这类批量赋值也要防御。同文件 `readCapabilityRoutes` 同类坑顺手修了。
- 验证：`go build ./plugins/admin-api/` ✓、`vue-tsc -b` ✓。

## 修复设置页 credentials tab 空数据崩溃（同类 nil length bug）

- 症状：`ManagementView.vue:177 TypeError: Cannot read properties of null (reading 'length')`（credentials tab）+ reka-ui 伴随警告。
- 根因：`handleKeysList` 返回 `{"sk_keys": ..., "mcp_keys": ...}`，`ListAPIKeys()`（gateway-keys manager，JSON 文件不存在时 nil）和 `readMCPKeys`（ErrNotExist 分支 `return nil, nil`）空数据时都是 nil slice → JSON `null`。模板 `keys?.sk_keys.length` 只保护了 keys 对象，字段级 null 照样炸。
- 修复：
  - 后端 `service.go handleKeysList`：`skKeys`/`mcpKeys` nil 统一规范化空 slice；`admin_helpers.go readMCPKeys` ErrNotExist 分支返回 `[]types.MCPKey{}`（同上次 readMCPServers 模式）。
  - 前端 `ManagementView.vue`：`keys?.sk_keys?.length`（v-if）、`keys?.sk_keys || []`（v-for）、`plugins?.plugins?.length || 0`（计数）、`plugins?.plugins?.length`（v-if）、`plugins?.plugins || []`（v-for）。
- 排查确认安全：`plugins` 数组后端初始化为空 slice；`CheckResult.Issues` 在 `core/plugin/loader.go ChecksByPlugin` 已做 nil→[] 规范化，`check.issues.length` 无需改。
- lesson：Go 返回 map 里嵌 slice 字段时，字段级 nil 同样编码为 null；前端对「对象.数组字段.length」要用 `?.` 链到数组本身。
- 验证：`go build ./plugins/admin-api/` ✓、`vue-tsc -b` ✓。

## 修复分页组件 NaN/undefined value 报错（DataPagination + shadcn-vue-cdn 语义坑）

- 症状：路由日志页（RouteLogsView）每次自动刷新（setInterval → useListLoader data.value 变化 → 重渲染）控制台刷 `Invalid prop: type check failed for prop "value". Expected Number with value NaN, got Undefined`，栈指向 PaginationListItem value=undefined。
- 根因：**shadcn-vue-cdn 包的 PaginationItem 渲染的是 PaginationListItem（reka-ui，`value: {type: Number, required: true}`）**，不是官方 shadcn 的 `<li>` 容器。`DataPagination.vue` 用官方 shadcn 惯例写了 4 个「PaginationItem 包 PaginationFirst/Previous/Next/Last」——这 4 个 PaginationItem 没传 value → 透传 undefined 给 PaginationListItem → 必填 Number 校验炸。页码项 PaginationItem 有 `:value="item.value"` 所以不报。
- 修复：`DataPagination.vue` 去掉 4 个导航按钮的 PaginationItem 包裹（PaginationFirst/Previous/Next/Last 是独立组件，直接放 PaginationContent 内，靠 PaginationRoot provide 取上下文），只给真正的页码项保留 PaginationItem（带 value）。
- lesson：本项目的 shadcn-vue-cdn（imohuan 的包）PaginationItem = PaginationListItem 语义（按钮 + value 必填），**不要按官方 shadcn-vue 文档那样用 PaginationItem 包导航按钮**。PaginationList 的 items 只有 page/ellipsis 两种（Gh/Hh/Uh，reka-ui utils），pageCount=1 时正常返回 [1]。
- 验证：`vue-tsc -b` ✓。

## 视觉日志：两阶段写入重构（v3，用户反馈日志时机）

用户反馈：识别完成才出现日志（视觉识别阻塞在主请求里，flush 在 hook 后）。要求访问时写占位、响应结束后更新状态。

重构（去掉 v2 的 Metadata 暂存+flush 机制，改为直接两阶段写）：
- route-log Start SQL UPSERT 增加 requested_model/virtual_model 更新（占位 + hook 后补全虚拟模型）
- model-gateway HandleProxy 在 before-upstream hook 前调用 proxyBeginLog 写占位 running；删除 flushVisionAttempts；proxyRejectedLog 保留幂等 Start + Finish
- vision 插件注入 route-log（SetRouteLog）；HandleProxyBeforeUpstream 识别开始写 running 占位（step_no=-(n-idx)），结束更新 success/failed
- contracts 删除 VisionAttemptLog/MetadataKeyVisionAttempts（v2 机制废弃）

测试：vision TestHandleProxyBeforeUpstreamVisionLogTwoPhase/MultiCandidate（mock routeLog 断言 running→终态序列）；model-gateway TestHandleProxyStartBeforeHook/TestHandleProxyVirtualModelSecondStart；TestHandleProxyRejectedBeforeUpstreamWritesLog 断言改为 starts=2（占位+补全）。

## 聚合/视觉渠道级路由：code-review + 浏览器实测（修复 6 处）

- code-reviewer 子代理审出 Critical：findAggregate DB 分支拷贝 target 丢 ChannelIDs/ChannelBaseURL → SQLite 生产模式下渠道级目标全 503。已修 + 补 DB 模式回归测试（TestFindAggregateDBChannelLevel）。
- 修 FK 坑（review 测试暴露）：渠道级 target channel_id="" 空字符串被 SQLite 外键当有效值 → FK 失败。ReplaceAggregates + importAggregates 空 channel_id 改写 NULL；ListAggregates 用 sql.NullString 读。
- 修路由越界：proxyForward 三档过滤空结果不回退默认候选（聚合语义：指定渠道不可用 → failover 换 target）；vision describeViaOption 显式渠道形态候选为空时报错，不静默自动路由。
- 修 Key 多选行为：selectAvailableTarget 对 ChannelIDs 多选也返回 candidates（组内一次 failover），不再逐次失败重发（__retry_count 10 次上限内）；applyTargetMetadata 同步改判断。
- 修前端交互 bug：ModelChannelList 原 deep watch model 数组 → toggle Key 改 item 属性触发 watch → 重置 open 数组 → popover 闪关。改 watch 仅监听 model.value.length。
- 修跨组误折叠：applySelection 折叠为渠道级需勾选集恰好等于某组全部 Key（ids.size === groupIds.length），避免 A 组全勾+B 组勾选时误折叠丢 Key。
- Chrome MCP 实测（localhost:5173）：聚合编辑/视觉候选编辑都验证——分组 tag 网格（NewAPi·1/像素星空·3 等）、跨组多选保持打开、整组全勾折叠为「像素星空（3 个 Key 轮询）」、保存落库 channel_base_url、老数据 channel_id 兼容显示。测试后已恢复被改动的真实数据（auto-demo target 0、vision 候选 1）。
- 环境：kill 3000 → powershell build.ps1（go build -buildvcs=false）→ Start-Process 后台启动 bin/loadout.exe（禁止前台）。

## 聚合/视觉渠道级路由：显式「选渠道」交互（v2）

- 用户反馈：逐 Key 勾选不够直观，要求显式选"渠道"（未来新增 Key 自动包含）。
- 改 ModelChannelList.vue：组标题从纯文字 `<p>` 改为可点击 `<button>`（hover 高亮 + tooltip「点击选择整个渠道（含未来新增的 Key）」），点击 = 整组 toggle（全未勾→全勾折叠为渠道级；有勾→全取消）。
- 验证「选渠道 → 模型候选 = 组内所有 Key 并集」：像素星空组 claude2(9)/claude(9)/gpt(6) → 选渠道后候选 15 个（9 Claude + codex-auto-review + 5 GPT）= 三 Key 并集，UI 实测通过。后端 ExpandCandidateKeys 渠道级按 base_url 动态展开，新增 Key 自动纳入，无需改后端。
- 注意：Chrome 实测中途 net::ERR_INTERNET_DISCONNECTED（Chrome 网络栈临时故障，curl 正常），未影响验证结论。

## 聚合目标渠道选择：单渠道约束（v3，用户澄清）

- 用户澄清：一个目标只能绑定**一个渠道（base_url 组）**，组内可多选 Key；**禁止跨渠道混选**。
- 改 ModelChannelList.vue：新增 activeGroupOf（当前勾选所属组）；toggleKey/toggleGroup 加「切组」语义——勾另一组 Key/点其他组标题时清空旧组选择，只保留新组；channelTriggerLabel 组内部分勾显示「渠道名 · Key1、Key2」，整组显示「渠道名（N 个 Key 轮询）」（去掉 v2 的跨组合并显示）。
- 实测：切组（newapi→像素星空 claude2 清空 newapi）、组内多选折叠（claude2+claude+gpt→像素星空（3 个 Key 轮询））、组标题切组（像素星空→NewAPi）全部通过。
- **数据污染事故与恢复**：浏览器测试期间 auto-demo 曾被覆盖成只剩 2 个 target（kimi/qwen），claude-haiku/claude-sonnet 丢失。已用 API PUT 恢复 4 个原始 target（均 @b89c685dac7402fd），并用 evaluate 确认后端数据完好。教训：浏览器交互测试要在测试后立即用 API 核对并恢复真实数据。

## 聚合渠道选择：渠道 vs Key 视觉区分（v4）

- 用户反馈：渠道名和 Key 名相同时（volcengine/volcengine）选完分不清是"选整组渠道"还是"单选 Key"。
- 改 ModelChannelList.vue：组标题 button 在整组已选时加底色+边框（`border-primary bg-primary/15 text-primary`），左侧加层叠图标（RiStackLine）+ 右侧 ✓；未选时只显示暗色钥匙图标（RiKey2Line），形态/图标/填充区分明显。
- 顺手修：channelTriggerLabel 部分勾时去重（Key 名=渠道名时不再显示 "volcengine · volcengine"）。
- 实测 DOM：选中 NewAPi 组 → `border-primary bg-primary/15 text-primary` + checked=true；未选组 → `border-transparent text-muted-foreground`。截图确认 UI 区分清晰。
- **用户实际操作保存数据**：用户 14:49 截图后自己操作保存了 auto-demo（目标 1 改成 `claude-fable-5 @ newapi` 渠道级），属于用户主动改动，**不再擅自恢复**避免覆盖。



注意：route-log Start 现在可能调用 2 次（占位+补全），UPSERT 按 request_id 合并；Start 断言类测试需按 2 次理解。

## step_no 改为单调递增正数空间（v4，用户反馈）

用户反馈：视觉 attempt 用 -1/-2/-3 序号反直觉，应该是 1, 2, 3, 4 连续递增（"第一步应该就是一啊"）。

修复：视觉 step_no = idx+1（正数，与主链路共享单调递增空间）；视觉循环结束设 pipe.Metadata["__route_step"]=visionStep；主链路 proxyAttemptLog 从 __route_step +1 续接；action 判断改用独立的 __main_route_step 计数器（避免视觉 step 占用导致首次尝试被误判为「切换渠道」）。

bug：成功路径 return pipe, nil 在循环内，跳过了循环外的 __route_step 写入，导致主链路从 1 起步与视觉 step=1 冲突。修复：成功 return 前也写入。

测试断言全部改正数：vision 单候选 step=1、多候选 1,2；E2E 单候选视觉 step=1 主链路 step=2。

## 前端详情重试（v4）

RouteLogsView.refreshActiveDetails 之前只刷 running，终态不再刷详情，导致详情首次延迟/失败时 UI 长期不完整。新增 shouldRefreshDetail：终态 + attempts 空 → 重试最多 5 次（detailRetryCount Map 计数）；拉取成功且 attempts 非空则清零。

## v4 review 修复

code-reviewer 结论：需返工（无 P0）。已修：
- P1-1 纯历史旧图 step 空悬：visionLogging 提到循环前，visionStep/__route_step 只在 visionLogging 时更新；纯旧图不占 step 空间（主链路从 1 开始）
- P1-2 前端无限重试：refreshActiveDetails 成功后终态 attempts 仍空也计数；达 5 次 expandedIds.delete 收起；注意 for 内用 continue 不是 return（return 会退出整个函数）
- P1-3 测试：新增 TestHandleProxyBeforeUpstreamPureHistoryNoStepPollution（纯旧图不写 attempt、不占 step）
- P2 expand() 补 catch（首次失败由定时刷新兜底）
- 未补（记录缺口）：视觉全失败+aggregate skipped 续接、aggregate 递归 failover 跨轮次 step——因场景组合罕见，且已由 TestHandleProxyRejectedBeforeUpstreamWritesLog 覆盖 aggregate 侧

## 渠道列表 loading 粒度：全局 → 按钮级

用户反馈：渠道页点任一按钮，整表按钮全部 disabled（原来 ChannelsView 单 useAsyncTask 共用一个 pending，ChannelTable 每个按钮 :disabled="pending"）。

后台并发调研结论（决定粒度上限）：
- `core/db/db.go`：SetMaxOpenConns(1) + WAL + busy_timeout=5000 → 并发写自动排队串行，不坏数据、不丢写，**支持按钮级并发**。
- 各操作写不同数据域（PATCH manual_enabled vs POST refresh-models 写 channel_models vs reorder 整表），跨 key 无共享状态。
- 唯一竞态：同一 key「刷新模型」×「编辑保存」都整写 channel_models，最后写者赢；刷新探测默认全 Enabled 会覆盖刚保存的禁用态。处理：编辑器保存用全局 key `save`（保存期间表格操作仍可用，但编辑器自己锁）。

实现：
- `useAsyncTask.ts`：单布尔 pending → `Set<string>` pendingKeys + `pending`(computed size>0，兼容旧调用方) + `isPending(key)`；`run` 重载 `run(task, success?)`（走 `__global__` key，向后兼容 9 个旧调用方）和 `run(key, task, success?)`。顺带修了旧实现并发时先完成任务提前解锁 UI 的 bug。
- `ChannelsView.vue`：groupKey(`group:{baseUrl}:{action}`)/keyKey(`key:{id}:{action}`) 两套 key；save 用 `run('save', ...)`；ChannelEditor :pending 改 isPending('save')；ChannelTable 传 `:is-pending="isPending"`。
- `ChannelTable.vue`：props pending? 改 isPending?；每个按钮 :disabled="busy(对应key)"，图标 pending 时换 RiLoader4Line animate-spin，停用/启用按钮显示「处理中」；编辑/添加 Key 不锁（只开弹窗）。
- 验证：vue-tsc --noEmit 零错误。
- lesson：按钮级 loading 需「run 的 key」与「按钮 disabled 的 key」同一套规则，否则对不上；组 key 里 baseUrl 要用 normalizeBaseURL 保证一致。

## 模型测试请求改「旁路探针」：不再写转发日志（用户需求）

用户需求：测试页点发送不应污染转发日志；只有调 Loadout 自己导出的服务（router 正常入口）才写日志。测试请求的访问摘要随响应回带，前端「请求记录」面板直显。

实施（后端 test_proxy.go + service.go，前端 useModelTest.ts + ModelTestView.vue）：
- `handleTestChat` 5 个分支的 `logTestResult` 全部删除（函数一并删），测试请求**永不调用 routeLog.Start/Attempt/Finish**。
- 新增访问摘要回带：非流式/错误路径用响应头 `X-Test-Log`（base64 JSON）；流式成功路径在 SSE 末尾追加 `event: route_log` 事件（data=base64 JSON）。摘要结构 `testLogSummary` 与前端 RouteLog 同形（含 attempts[0]，failure_class 只在 attempt 层）。
- 前端 `useModelTest.chat()` 第 5 参改为 options 对象（onDelta/onSummary/signal）；SSE 解析器识别 event 行 + route_log 事件；`decodeTestLogSummary` 用 atob + TextDecoder(utf-8) 解码（error_message 可能含中文）。`syncTestLog` 双源兜底：先用 summary 完整化 entry，再用 request_id 探测 detail 覆盖（上游是 Loadout 自家服务时能拉到）。
- 文案同步：ModelTestView PageHeader / service.go / test_proxy.go 注释全部去掉「测试请求会写入转发日志」。
- 测试：新增 test_proxy_summary_test.go（newRouteLogTestServer 带真实 routeLog 服务）：TestModelTestNoRouteLogWritten（非流式+流式后 route_requests 表必须 0 行）、TestModelTestStreamSummaryEvent、TestModelTestErrorPathSummaryHeader；TestModelTestProxy 增强 X-Test-Log 断言。go test ./plugins/admin-api/ 全绿。

## 模型测试旁路改造：code review + 修复

code-reviewer 审查结论：无 P0，可合入，修 2 个 P1。已修：
- P1-1 错误路径重复行：chat() throw 前 onSummary 已写入真实 request_id，但 catch 分支 entry.request_id 还是占位 test-xxx → syncTestLog 按真实 id 找不到占位行 → unshift 出两行。修：catch 用 summaryHolder 覆盖 request_id + syncTestLog 按对象引用（log === entry）兜底定位。
- P1-2 abort 无摘要丢失持久化：旧代码 catch 有 saveTestRouteLog(entry)，重构后丢了。修：syncTestLog catch 分支补 `if (!summary) saveTestRouteLog(entry)`。
- P2-3 并发加固：send() 顶部加 `if (streaming.value) return` 硬保护；testSummary 共享 ref 改为 send() 闭包内 `summaryHolder = { value: RouteLog | null }` 对象容器（对象属性不被 TS 控制流窄化）。
- P2-5 decoder flush：SSE 解析重构为 processLines() 局部函数，done 时 `buffer += decoder.decode()` flush 多字节残留后补扫。
- P2-6 rune 安全截断：test_proxy.go error_message 截断改 `[]rune` 切片（避免切在多字节字符中间）。
- P2-7 补测试：TestModelTestNoRouteLogWritten 加错误路径（上游 401）不写日志断言。
- 未修（接受）：P2-4 流式预置 header（有 running 误导风险，reviewer 也认为非必须）；P2-8 既有 300s 流式超时 / toISOString 排序小问题。
- TS 坑（最小复现确认）：**try-catch 中变量在闭包（onSummary 回调）内赋值，catch 分支读取该局部变量时 TS 保守窄化为初始值 null → 可选链取属性报 "Property does not exist on type 'never'"**。标准 TS 行为（非 vue-tsc bug）。绕过：用对象容器 `{ value: T | null }` 持值（对象属性不做控制流窄化）。
- 验证：go build ./... ✓、go test ./plugins/admin-api/ 全绿 ✓、vue-tsc -b --force --noEmit 0 错误 ✓。
- 顺手修：vue-tsc -b 增量缓存陈旧误报 useMcpManagement isPending（--force 全量重建后 0 错误，非真实问题）；useMcpManagement.ts 未改动。
- lesson：测试环境 newTestServer 没接 routeLog（s.routeLog==nil），旧测试从未真正验证过「写日志」；验证不写日志须自建带 routeLog 的测试服务器。

## 按钮级 loading 推广到全前端（模型状态/转发日志/MCP/技能/能力路由/聚合/设置）

用户反馈按钮看不到加载状态 → 根因：本地写操作毫秒级完成，spinner 一帧都画不出。`useAsyncTask.run` 加 `MIN_PENDING_MS=300` 最小展示时长（成功路径补齐，失败不补，toast 即时反馈）。

随后按同样模式推广所有页面：
- **模型状态**（ModelStatusView + List/ChannelGroup/Channel + Filters）：key 规则 `ms:{channelId}:{action}`（toggle/model-toggle:name/model-delete:name/batch-toggle:on|off/batch-recover/batch-delete/recover/model-recover:name/recover-all/check/recover-all-channels/recover-all-models/refresh/filter）
- **转发日志**（RouteLogsView + RouteLogFilters）：`refresh`/`clear`/`apply`
- **MCP 管理**（useMcpManagement + McpPanel）：`server:{id}:test|toggle|remove`、`group:{name}:remove`、`save-server`/`save-group`、端点密钥 `endpoint:{path}:create|delete`（McpPanel 独立 useAsyncTask 实例）
- **技能**（SkillsView）：`install-skill`/`import-skill`/`save-preset`/`skill:{name}:remove`/`preset:{name}:apply|remove`/`platform:{name}:restore`/`restore-all`；顶部刷新/同步/更新用各自已有状态（anyRefreshing/syncing/updateStatus）
- **能力路由**（CapabilityRoutesView + Table）：`save` + `route:{key}:remove`（key=capability|models|channel_ids，view 与 table 各复制一份同规则）
- **聚合**（AggregatesView + Table）：`save` + `aggregate:{name}:remove`
- **设置**（ManagementView）：`refresh`/`create-key`/`key:{id}:remove`/`save-settings`/`change-password`；导出/导入按钮去 pending 锁（仅开弹窗）
- 通用模式：view 用 `const { run, isPending } = useAsyncTask()`，子组件 props 改 `isPending?: (key)=>boolean` + 本地 `busy(key)` helper；图标按钮 pending 时换 RiLoader4Line animate-spin，文字按钮加 spinner + 文案切换；纯开弹窗的按钮（编辑/添加）不锁
- 遗留：LoginView/Login.vue 保持全局 pending（登录表单本来就该整体锁）；ChannelEditor/CapabilityRouteEditor/AggregateEditor 的 pending prop 由父级传 isPending('save') 等布尔值，无需改
- 验证：vue-tsc --noEmit 零错误
- 渠道编辑器加「获取模型」按钮：新增 POST /api/channels/probe（不落库）。新建传 base_url+api_key；编辑时 Key 不回显只传 id，后端按 id 取已存 Key 探测。前端 useChannels.probe() + ChannelEditor 搜索框右侧按钮，新探测模型默认启用并入候选池，失败显示 probeError。go build ./... 通过；TestChannelProbe 覆盖新建/编辑/无效id/缺base_url；vue-tsc -b --force 0 错误。
- 渠道分级（方案 B）：新增渠道名称 channel_name（Base URL 组级字段，同组 Key 冗余同步）。
  后端：types.Channel + db.Channel 加 ChannelName；db 迁移 v6 ALTER channels ADD channel_name；创建渠道显式传 channel_name，未传则继承同 base_url 已有渠道名（添加 Key 场景）或回退 Key 名；编辑任一 Key 改渠道名 → 同 base_url 全部同步（文件模式 handleChannelUpdate + DB 模式 handleChannelUpdateDB 均实现）。前端：ChannelEditor 加「渠道名称」输入框（添加 Key 模式禁用+展示 groupName，ChannelsView openAddKey 取同组首个 channel_name 传入）；ChannelTable group 行显示 channel_name 兜底 name + BaseURL；ModelTestView 预设下拉按 base_url 分组（SelectLabel 渠道名 + SelectItem Key 名）。TestChannelNameSync 覆盖创建/继承/同步/隔离。go build + go test 全绿；vue-tsc --force 0 错误。
- code review 修复（probe + channel_name）：① probe 解密失败静默吞掉→改为 500；cipher 空误判 404→区分"渠道不存在"(404) 与"渠道无 Key"(空 key 探测)；② 编辑 Key 挪组（改 base_url）时前端回传旧组名→后端强制跟随新组名（文件+DB 双模式），避免同组不一致；③ 添加 Key 模式前端不再提交 channel_name（后端继承），修复旧数据组内分叉；④ 文件模式 create 补 name/base_url 必填校验（与 DB 模式对齐）。新增 TestChannelMoveGroupName。已知限制（未修）：channel_name 空串=未修改，无法清空渠道名；api_key_cipher 密文回传列表（既有行为）；List→Replace 全量替换并发丢更新（既有架构）；probe 用 context.Background()（既有 probeChannelModels 限制）；新测试仅覆盖文件模式。
- 模型状态页 group 行加渠道名：ModelStatusChannelGroup 加 groupTitle computed（channel.channel_name 兜底 channel.name 兜底 baseUrl），模板第一行展示标题、第二行展示 Base URL（与 ChannelTable 同套路）。vue-tsc --force 通过。
- 修复模型状态页渠道名不显示：根因是数据链路缺 channel_name——contracts.ChannelStatus 无该字段 → model-health listChannelStatus SQL 未查 c.channel_name → admin-api flattenStatus 未透传。四处补齐：contracts/routing.go 加 ChannelName、model-health service.go（channelStatusRow + SQL + Scan + List 构造）、admin-api flattenStatus map 加 channel_name。go build + admin-api/model-health 测试全绿。
- 模型测试「Loadout 自带」预设：SK key 创建时用 store.Encrypt 加密明文存 APIKey.Cipher（types.APIKey 加字段；db migration v7 gateway_keys.api_key_cipher；ListAPIKeys 列表接口剥掉 Cipher 不回传）。前端传 sk_key_hash → 后端 ResolveAPIKey 按 hash 匹配记录 + Decrypt 得明文 → 配 base_url 直调自家网关（test_proxy resolveTestTarget 新增分支）。前端：预设下拉顶部加「Loadout 自带 API」（channelId='__builtin__' 哨兵），API Key 输入框在自带模式变 Select（列 sk_keys name+prefix，选 hash）；buildTarget 带 sk_key_hash。TestModelTestBuiltinSkKey 覆盖。db_test 迁移数量 5→7、未来版本 6→8。
- SK key 自带预设 code review 修复：① Critical：sk_key_hash 分支 base_url 未校验（攻击者可指向自己服务器骗明文）→ resolveTestTarget 强制 base_url host == r.Host 或本地回环（isLoopbackHost），否则 400；② 前端 presetTriggerLabel 识别 __builtin__ 显示「Loadout 自带 API」；③ skKeys 过滤 enabled！== false；④ fetchModels/send 自带模式未选 key 前置报错；⑤ 测试补恶意 base_url 400 + 删除 key 后 hash 失效 400。已知不改：ReplaceAPIKeys 静默 fallback JSON（既有行为）、APIKeyByHash 读路径不持锁（单写者低风险）、旧数据无 Cipher 需重建 key、跨机拷贝数据目录解密失败（文档级限制）。
- 自带模式 base_url 改后端自动补全：前端不再传 base_url（自带模式 Base URL 输入框隐藏 + 说明文案），后端 resolveTestTarget sk_key_hash 分支用 r.Host 构造 scheme://host/v1（TLS 判断 scheme），彻底移除用户可控 base_url（连带删掉之前加的 host 校验 isLoopbackHost）。TestModelTestBuiltinSkKey 重构为 combined server（admin + /v1/models 同 host）验证自动补全路径。
- 自带模式 base_url 双规则：前端保留 Base URL 输入框（默认 /v1 + placeholder 提示），相对路径/空 → 后端按请求 Host 补全 scheme://Host；完整 http(s):// URL → 直接使用（支持自定义域名测试）。识别自带模式仍靠 sk_key_hash（与 base_url 无关）。TestModelTestBuiltinSkKey 补相对路径 + 完整 URL 两场景。
- 请求记录 UI 优化：① 自带模式「最终目标」列显示 @ 自带 · {key名}（testLogSummary 加 SkKeyName 字段，manager.ResolveAPIKey 改返回 (plain, name, error)，resolveTestTarget 签名加 skKeyName 透传，RouteLogTable final_channel_id==='__builtin__' 时用 finalTargetLabel 渲染）；② 结果列不再走 StatusBadge（其"已关闭"覆盖逻辑会污染测试结果语义），自渲染中文 RESULT_LABELS（已成功/失败/进行中/已跳过/已中断）+ 颜色 resultTone。

## UnifyAI 配置同步页面（前端 UI，后台未接入）

按 D:\Code\Git\unifyai\docs\UI-DESIGN-SPEC.md 在 loadout frontend 实现单页工具型 UI，全链路只做前端 + 预留函数：
- 新增文件：`src/lib/unifyai.ts`（类型 + PLATFORMS 能力矩阵 + INITIAL_MCP_SERVERS mock + buildCommand CLI 拼装 + fetchModelSource/fetchMcpServers/updateMetadata/runSync 预留签名，均 console.warn 返回 mock）；`src/components/unifyai/PlatformCard.vue`（能力徽章/选中态/置灰）；`ExcludeMatrix.vue`（排除矩阵三态单元格 + 全局/按平台排除推导 + 白名单 Tab + 只显示被排除项开关）；`CommandPreview.vue`（模型/MCP 来源状态条 + 实时命令预览）；`UnifyaiPanel.vue`（同步内容三态 Tabs、平台卡片、执行前确认弹窗 §10.3、模拟执行日志流含 §8 图标语义/着色/自动滚动、平台 chips 状态、汇总卡片）；`src/views/UnifyaiView.vue`（薄包装）。
- 路由 `/unifyai`（title UnifyAI 配置同步）+ 侧边栏「配置」组新增「UnifyAI 同步」项（RiSwapLine）。
- 默认交互预置文档典型用例：node_env 对 Codex 默认排除；模式切「仅模型」时 MCP 过滤区隐藏、不支持该能力的平台卡片置灰。
- 注意：Badge variant 无 warning（用 outline）；tsconfig 开启 noUnusedLocals/erasableSyntaxOnly（禁 enum）；`buildCommand` 全局排除不重复进 per-platform 列表。
- 验证：vue-tsc 0 错误 + pnpm build 通过（UnifyaiView chunk ~30KB）。后端接入点：lib/unifyai.ts 内 TODO(backend) 函数。
- 第二轮 review 修复：① RouteLogTable isExpanded 只管折叠集合、showDetails 收敛非折叠逻辑（collapsible=false 恒 false，用户明确要求隐藏详情区）；② test_proxy 相对 base_url 必须 / 开头且不带 query（防裸域名/query 拼接错位，非法输入 400）；③ builtinChannelID 收敛：前端建 lib/constants.ts 导出 BUILTIN_CHANNEL（RouteLogTable/ModelTestView 共用），后端 test_proxy.go 单点定义。权衡说明：collapsible=false 时错误详情也隐藏（用户要求），测试失败信息仍在响应面板可见。

## UnifyAI 配置同步页面 v2：补基础交互流程

按用户反馈「除开始同步/dry-run 2 个按钮外其他功能都实现」+ 修复交互 bug：

**修复 3 个真 bug：**
- ① ExcludeMatrix `structuredClone(props.matrix)` 抛 `DataCloneError`（Vue props 是只读代理，structuredClone 无法克隆），导致排除矩阵单元格/「排除全部」点击完全无反应、console 报 DataCloneError 被吞。修：cloneMatrix() 用 Object.fromEntries + spread 显式浅拷贝。
- ② shadcn-vue-cdn 的 Switch 对外接口是 `modelValue + update:modelValue`（`checked` 只是 slot thumb 的类型声明），`:checked @update:checked` 与 `v-model:checked` 都无效。修：4 个 Switch（--all/verbose/只显示被排除项/白名单开关）改 `v-model`；白名单在子组件用 computed get/set 包装回写父组件。
- ③ 模式切换会永久剔除不支持能力的平台（仅模型 → 全部同步 后 Codex/Claude Code 永久丢失）。修：删除剔除逻辑，平台卡 mode 不支持时置灰但保留选中（执行时跳过并提示），符合文档 §6.3。

**补齐交互：**
- 顶部 5 步使用流程引导条（可折叠）+ 6 个 Card 标题加编号 ① ② ③ ④ ⑤ ⑥
- 命令预览「复制命令」按钮 → 剪贴板 + toast
- 「更新元数据」按钮 → loading + delay 500ms + toast + 模型来源卡片显示「元数据缓存：{时间} 已刷新」
- 白名单 Tab 独立 `whitelistEnabled` 开关（语义「限定/不限定」），开+空 = 警告「所有平台 MCP 同步将被跳过」；chips 全选 = 不生成冗余 --mcp-platforms
- 排除矩阵表格空态（只显示被排除项时无数据 → 友好提示）
- 高级选项折叠区（source 路径 + verbose 开关）
- 日志区「清空」按钮 + Card 标题前缀「⑥ 执行结果」/「⑥ 预览结果（未写入任何文件）」
- toast 全程 vue-sonner 反馈

**验收：** vue-tsc 0 错误、pnpm build 通过；Chrome MCP 验证完整链路：模式切换（仅模型 → MCP 过滤区消失 + Codex/Claude Code 置灰 + 命令加 --models-only）/ 平台卡 toggle / 排除矩阵单元格（按平台）/ 「排除全部」（全局 --mcp-exclude 不与 per-platform 重复）/ --all（命令变 --all）/ 白名单 tab+开关+chips（开+勾 Codex → --mcp-platforms codex）/ 复制命令 / 更新元数据 toast / 开始同步（确认弹窗 3 条警告 → 备份 .bak-{时间戳} → 逐平台 → 汇总：成功 5 / 失败 0 / 372 模型 / 4 MCP）。截图：unifyai-page.png（初始）+ unifyai-after-sync.png（执行后）。

**Chrome MCP 验证踩坑：**
- 多 tab 环境下 evaluate_script 会漂移（连 tab 2 model-test 都可能被命中），即使 select_page 后也漂移；select + navigate + 立即 evaluate/screenshot 一气呵成。isolatedContext 开新页面无登录态被路由守卫拦到 /login。
- reka-ui Tabs/Switch 对 synthetic click（`el.click()`）不响应，必须用 Chrome MCP `click` 工具走真实 CDP 事件序列；普通 button 两者皆可。
- `pnpm dev | head -20` 管道会被 SIGPIPE 杀掉，必须重定向到文件或 run_in_background。

**复用价值（沉淀）：**
- shadcn-vue-cdn Switch 用 v-model（modelValue）非 checked；`v-model:checked` / `:checked/@update:checked` 都是常见错写。
- Vue props 是只读代理，`structuredClone(props)` 必抛 DataCloneError；复制受控表单状态用 `Object.fromEntries(Object.entries(x).map(([k,v]) => [k, {...v}]))` 或 `JSON.parse(JSON.stringify(x))`。

## UnifyAI 页面 v3 调整（按截图反馈）

按用户截图标注的 5 处调整：
- 删：「使用流程（5 步）」引导条 + 「① 同步内容」Card
- 合并：模式 Tabs（全部同步/仅模型/仅 MCP） + 「全部平台」Switch 移到 ② 目标平台 Card 标题右侧
- 紧凑：平台卡片删「格式」行（p-3→p-2、品牌方块 6→5、徽章 11px→10px）；5 列 inline `grid-template-columns: repeat(5, minmax(0, 1fr))` 强制（绕开 Tailwind 响应式未生效问题）
- 删：③ MCP 同步过滤内「排除矩阵 / MCP 目标平台白名单」Tabs 切换
- 展开：排除矩阵 + 白名单 chips + 开关在 ③ Card 内直接并列展示（`<Separator />` 分隔）

**踩坑：**
- shadcn-vue Tabs 组件在 `flex` 父容器内被压缩（`shrink` 默认 1），即使 `w-[300px] + shrink-0` 也被裁切（可能全局 Tabs 样式有 max-width）。改用纯按钮组（`inline-flex h-8 border p-0.5` + 3 个 button 模拟 segmented control），完全可控不依赖第三方组件。
- 平台卡片 grid 想用 `lg:grid-cols-4 xl:grid-cols-5` 但 Tailwind v4 响应式类只生成了项目其他地方用过的 `xl:grid-cols-4`，新引入的 `xl:grid-cols-5 / md:grid-cols-3` 没生成。**根本原因排查**：style.css 原本只 `@import 'tailwindcss'` 没声明源码源，Tailwind v4 不会扫 .vue 文件（只扫 html/js/css）；加 `@source './**/*.{vue,js,ts,jsx,tsx}'` 后基础类生成了，但响应式类仍未生成（疑似 scanner 缓存或 Tailwind v4 + 插件配置问题）。**简化方案**：直接 `style="grid-template-columns: repeat(5, minmax(0, 1fr))"` 强制 5 列，绕开 grid-cols-N 响应式。
- 顺带发现 dev server 因 5199 端口被占用 fallback 到 5200，Chrome MCP 之前访问 5199 命中老进程，HMR 不同步 → 改访问 5200 后看到正确效果。

**沉淀到工作区 memory（待同步 MEMORY.md）：** Tailwind v4 项目需在 style.css 加 `@source './**/*.{vue,...}'` 显式声明源码源；新建 `pnpm dev` 注意端口 fallback（已用 `pnpm dev > log 2>&1` 不用 head 防 SIGPIPE，但端口冲突要查实际端口）。

vue-tsc 0 错误，pnpm build 通过，截图 unifyai-v3-redesign.png 展示新布局。

## UnifyAI v4：Tabs 改用 shadcn-vue + 命令合并逗号分隔

按用户反馈两处优化：
- ② 目标平台 Card 头部的模式三态从手写 segmented control 改为 shadcn-vue Tabs（参考 MCP 管理的 TabsList `inline-flex h-auto w-fit flex-wrap gap-1` + `variant="line"`），激活 tab 有下划线视觉强调。之前自己手写按钮组是因为 Tabs 在 flex 父容器被压缩，**这次用 shadcn-vue 默认 props（不强制 grid/固定宽）就正常显示**——MCP 管理的 Tabs 也是同写法。
- `lib/unifyai.ts` 的 `buildCommand` 把同平台多 server 合并到一个 `--mcp-exclude-for` 参数，用逗号分隔：原写法是每个 server 一行 `--mcp-exclude-for 平台=server`，新写法是 `--mcp-exclude-for 平台=server1,server2`。等价于 CLI 把逗号 split 后合并到该平台的排除 Set，行为完全一致。

**输出示例**（多平台多 server 混合排除）：
```
unifyai --platforms opencode,codex,claudecode,reasonix,penguin --mcp-exclude-for codex=filesystem,node_env,github,remote-server --mcp-exclude-for reasonix=node_env
```

**沉淀：** shadcn-vue Tabs 想要自然宽度 + 下划线激活态，**不要**手动加 `grid grid-cols-N w-[Xpx]`，会触发组件内部样式冲突被裁。直接 `inline-flex h-auto w-fit flex-wrap` + `variant="line"` 即可。TabsList 有 `variant: "default" | "line"` 两态（line 带下划线，参考 MCP 管理用的就是这个）。

## UnifyAI v5：disabled MCP 完整展示 + 启用/禁用切换

按用户反馈：排除矩阵表格必须完整显示所有 MCP（包括 disabled 的），并支持启用/禁用切换。

改动：
- `ExcludeMatrix` 接 `disabled: Set<string>` prop + `@update:disabled` emit；显示所有服务器（`:servers="allServers"` 不再是 enabledServers）。
- disabled 行视觉：整行 `opacity-50` + 「已禁用」徽章 + 单元格置灰（不可点击，`cursor-not-allowed`）+ 「排除全部/恢复全部」按钮 disabled。
- 操作列加切换按钮（绿色 toggle 图标 + 启用/禁用 tooltip）。
- `UnifyaiPanel` 新增 `disabledServers: ref<Set<string>>`，初始从 `INITIAL_MCP_SERVERS.filter(s => !s.enabled).map(s => s.name)` 提取；`enabledServers` computed 改为 `allServers.value.filter(s => !disabledServers.value.has(s.name))`；`initMatrix` 改为遍历 allServers 建占位（启用后状态干净）。
- `fetchMcpServers` mock 改为返回所有（含 disabled），UI 由 disabledServers 决定参与同步。
- 数据预览「MCP 来源」从 `4/5 启用` 动态反映实际启用数（4/5 或 5/5 切换）。

**图标坑：** `@remixicon/vue` 没有 `RiPowerOffLine / RiPowerOnLine`，改用 `RiToggleFill`（启用，emerald）+ `RiToggleLine`（禁用，muted）。

实测：legacy-search 初始禁用显示置灰；点「启用」→ `4/5` → `5/5`，命令不增加（无排除）；点「禁用」→ `5/5` → `4/5`，命令不减少（无排除）。`buildCommand` 用 enabledServers 过滤，disabled server 不进命令。vue-tsc 0 错误，pnpm build 通过。截图 `outputs/unifyai-v5-disabled.png`。

## 聚合模型 + 视觉候选：渠道级 / Key 多选路由（实施完成）

- 需求：聚合模型目标、能力路由视觉候选的渠道选择，支持「选渠道（base_url 组，自动轮询组内 Key）」和「选多个具体 Key」两种粒度；UI 沿用 ModelTestView 524-610 的分组 tag 风格。
- 设计：AggregateTarget / ViaOption 加 channel_base_url（渠道级）+ channel_ids[]（Key 多选），channel_id 保留兼容；前端 ModelChannelList 整组全勾 → 折叠为 channel_base_url（UI 高亮整组），部分勾 → channel_ids[]。
- 后端：migration 8 重建 aggregate_targets（channel_id 去 NOT NULL + channel_ids_json + channel_base_url 列）；model-gateway 新增 ExpandCandidateKeys 共享工具（渠道级展开/多选/单值/去重/归一化）；aggregate selectAvailableTarget 改返回 (target, candidates, err)，渠道级写 __channel_candidates 让 proxyForward 组内 failover；resolveChannels/proxyForward 支持 __channel_candidates / __current_channel_base_url / __current_channel 三档过滤；vision describeViaOption 复用展开逻辑逐 Key 尝试。
- 前端：ModelChannelList 重写（Popover + 渠道分组 tag 多选 + 整组全勾折叠）；AggregateEditor/AggregateTable/CapabilityRouteEditor/CapabilityRouteTable 适配；types.ts 三处接口加字段。
- 验证：go build + go test 全绿（唯一失败 core/linkfs 是 Windows symlink 环境问题，与改动无关）；vue-tsc 通过；vite build 通过（注意 NODE_OPTIONS="--use-system-ca" 绕过 WorkBuddy 删除拦截）。
- 计划文档：docs/plans/2026-08-20-aggregate-via-channel-group.md。

## 渠道名显示规范抽象（review 后收尾）

- 用户最终拍板格式：渠道级 `@NewAPi`（无后缀）；Key 单/多选统一 `@NewAPi(Key1)` / `@NewAPi(Key1, Key2)`（括号逗号分隔，单 Key 也带渠道名前缀）。
- 渠道级与 Key 全勾是**两个独立概念**：只有点渠道组标题才写 channel_base_url；Key 全部勾满仍是 channel_ids，不折叠、不触发渠道边框；边框/图标只由 channel_base_url === group.baseUrl 驱动。已删 ModelChannelList 里 enableChannelLevel 折叠逻辑与 prop（死代码）。
- 新增统一规范层：`frontend/src/composables/useChannelRef.ts`（formatChannelRef 纯函数 + ChannelRefInput 类型）+ `frontend/src/components/ChannelRef.vue`（展示组件，prop 名 target，atPrefix 默认 true）。
- 四处全部接入：ModelChannelList trigger（函数）、AggregateTable / CapabilityRouteTable / RouteLogTable（组件）。日志表只有单 Key（final_channel_id / attempts.channel_id），包成 {channel_id} 走同一规范。
- review 教训：Vue 组件 prop 不能用 `ref`（保留字，编译报错），用 target；同名类型别叫 ChannelRef（与组件撞名），改 ChannelRefInput；删旧逻辑后记得删失效 prop。
- vue-tsc / vite build 全绿（构建需 NODE_OPTIONS="--use-system-ca" 绕 WorkBuddy 删除拦截）。

## ModelChannelList tooltip 延迟显示（用户需求）

- 用户要求 tooltip 悬停 1s 后才显示。组件用的是 reka-ui（shadcn-vue-cdn）TooltipProvider/Tooltip 组合。
- 修复：ModelChannelList.vue 三个 TooltipProvider（上移/下移/移除）各加 `:delay-duration="1000"`（reka-ui TooltipProvider 默认 delayDuration=700，单位 ms）。无需改 Tooltip 本身。

## UnifyAI 后端接入：真实执行 unifyai（node 本地脚本）+ 通用日志组件

按用户需求「模型配置可通过 unifyai 指令配置，实现后台执行 unifyai 提供日志，参考技能页检查与更新，抽离通用组件」（测试交给用户）：

**后端（Go）：**
- 新插件 `plugins/unifyai/`（service.go + runner.go + plugin.go）：
  - `PlatformInfo()`：执行 `--list-platforms --json` 解析平台能力（附录 B.1 结构）；CLI 失败回落内置默认 5 平台
  - `Run(args, onLog)`：执行 unifyai，stdout/stderr 逐行回调
  - RunRunner 单实例任务 + 多订阅广播（复刻 skills/updater.go：log|done|error 事件、done 后关闭 channel）；args 经 SetArgs/takePendingArgs 传递
  - **resolveCmd 按用户要求走 node 本地脚本，不要用全局 CLI**：① config.UnifyaiDir（LOADOUT_UNIFYAI_DIR）② D:/Code/Git/unifyai 等仓库 src/cli.mjs → node 执行；③ 全局 unifyai 命令仅最后兜底。实测全局 CLI 是旧版 1.0.2 无 --json，本地 1.0.4 支持
- `core/config/config.go` 加 `UnifyaiDir`（LOADOUT_UNIFYAI_DIR）
- `plugins/registry.go` 注册 unifyai 插件
- admin-api 注入 unifyai + 3 条路由：`GET /api/unifyai/platforms`、`POST /api/unifyai/run` {args}、`GET /api/unifyai/stream?args=<json>`（**args 走 query 单调用，SSE 连接即启动任务，避免 POST/SSE 双调用竞态**）
- 测试：admin_api_test.go 等 5 个测试文件 NewService 签名批量补 unifyai 参数（sed）；新增 unifyai service_test.go（回落/流式/错误/单实例广播 4 用例全绿）

**前端：**
- 抽通用组件：`src/lib/ansi.ts`（ANSI→HTML，从 SkillsView 抽出）；`src/components/StreamLogPanel.vue`（通用 SSE 日志面板：streamUrl/trigger/status props + done(payload=exit code)/error 事件，自动滚动 + ANSI 着色 + 状态徽章 + 空态）
- SkillsView 重构复用：删本地 SSE/ANSI ~150 行，日志 Tab 换 StreamLogPanel，checkUpdates 改 updateTrigger++
- UnifyaiPanel 接真实后端：onMounted fetch platforms 更新平台卡（失败保持内置）；预览/开始同步 → `buildArgs`（lib/unifyai.ts 从 buildCommand 拆出数组版，命令预览与执行共用）→ streamUrl query → runTrigger++ 触发 StreamLogPanel；汇总卡片 done→退出码 0 成功；移除全部 mock 执行逻辑（runSimulated/buildExecutionLogs/platformPlan）

**验证：** go build ./... ✓、go test ./plugins/admin-api/ + ./plugins/unifyai/ 全绿 ✓、vue-tsc 0 错误 ✓、pnpm build ✓。隔离实例 curl 实测：platforms 返回附录 B.1 结构；stream 日志流显示 `执行: node D:\Code\Git\unifyai\src\cli.mjs --list-platforms --json`（--json 正常）；dry-run 真实输出逐行推送（OpenCodex 降级 → 27 个模型）。浏览器 E2E 未做（测试交给用户）。


## UnifyAI mcp.json 开关字段去重（用户反馈）

- 用户指出写回的 mcp.json 同时含 `enabled` 与 `disabled` 两个开关字段，冗余。
- 查证 unifyai 源码：同步时 `src/core/config-loader.mjs` 的 `loadMcpConfig` 以 `if (!config.disabled)` 过滤服务器（主判断字段），`enabled` 仅 `normalizeMcp`（`server.enabled !== false`）兜底。
- **保留 disabled、删除 enabled 双写**：SaveMcpServers 只写 `disabled: !srv.Enabled`。读取仍兼容两个字段（enabled===false || disabled===true → 禁用）。
- 顺带把 `mcpConfigPath` 函数改为包级变量（与 runCommandStream 同模式），新增测试 `TestSaveMcpServersSingleSwitchField` 断言写回不含 enabled 且 disabled 正确。unifyai/admin-api 测试全绿，go build ./... ✓，vue-tsc ✓。

## 渠道编辑弹窗 130+ 模型撑爆 UI 修复 + 全站弹层高度约束排查

- 症状：volcengine（130 模型）点编辑，dialog 内「已选模型 Badge 列表」无高度限制全部展开 → 整个 UI 变形。
- 根因：ChannelEditor.vue 的 DialogContent 缺 `max-h-[calc(100dvh-2rem)]`，且已选 badge 列表（v-if form.models.length 的 flex flex-wrap div）无 max-h + overflow-y-auto。popover 候选列表本身已有 max-h-56 overflow-y-auto（CapabilityRouteEditor 同款，不是问题）。
- 修复：ChannelEditor.vue ① DialogContent 加 `max-h-[calc(100dvh-2rem)] overflow-y-auto sm:max-w-2xl!`；② badge 列表加 `max-h-40 overflow-y-auto rounded-md border border-border p-2`。
- 全站排查 14 个 DialogContent + 弹层（含 Popover/Select/Command）：已有约束的（ModelTestView/AggregateEditor/CapabilityRouteEditor/McpPanel×4/UnifyaiPanel×2/SkillsView 预设弹窗/ModelChannelList popover max-h-80/ModelTestView 模型列表 max-h-64/McpPanel 工具区 max-h-55vh）；固定内容安全的（ManagementView 密钥/ConfirmDialog/ConfigExportDialog/UnifyaiPanel confirm/EditTool 占位）。唯一随外部输入增长且无约束的 ConfigImportDialog（sections 数量取决于 zip 包）→ 同样加 `max-h-[calc(100dvh-2rem)] overflow-y-auto`。
- lesson：凡 DialogContent 内容含「数量取决于数据/导入内容」的列表，一律加 max-h-[calc(100dvh-2rem)] overflow-y-auto（项目已有 10 处同款先例）；内部长列表再套一层 max-h-N + overflow-y-auto 才能扛住百级数据。固定表单弹窗（2 个 input 以内）可不加。

## 敏感词过滤（sensitive-filter）实现梳理 + 业界对比分析

- 用户询问：全局替换（含历史消息）是否合理。用 codegraph 梳理实现，WebSearch 查业界做法。
- 实现（plugins/sensitive-filter/service.go，入口 HandleProxyBeforeUpstream）：
  - body 非 JSON 直接透传；查能力路由表（model+channelID，fail-open）
  - RouteProxy：整体 string(body) → replaceAll 逐条（正则用 regexp.ReplaceAllString 支持 $1）→ json.Valid + 字节数变化双条件；失败（替换破坏 JSON 或字节未变，如中文被转义 \uXXXX）降级 replaceMessagesText：解析 JSON → 显式处理 messages/input 的 content（纯字符串 + 分段 text/input_text 块）→ replaceStringValues 递归兜底整棵树所有字符串值
  - RouteError：containsAny 整体扫描，命中即 sensitiveError 拒绝（type=sensitive_filter_error）
  - 空 from 跳过；正则编译失败返回 error 拒绝
- 业界结论（dev.to 网关内容安全文章 + OpenAI moderation 文档）：**全局检查合理且必要**。模型推理看整个上下文，只检查最新消息有盲区（例："之前提到的那个地区"无法追溯）；不可信客户端可篡改历史。全上下文检查是标准做法。
- 差距清单：① 缺输出端过滤（只在 ProxyBeforeUpstream 输入端；模型自己生成的敏感词会漏，OpenAI 强调 output moderation 是最后防线）；② error 模式全 body 扫描，历史偶发敏感词会让后续正常请求全被拒（业界折中：最新用户消息 block，历史 replace 或 log-and-pass/warn）；③ 无 log-and-pass 告警放行模式（业界 warn 起步再逐渐收紧）；④ 流式输出过滤需 delay buffer 防跨 chunk 拼接（若后续做输出端）。
- 用户结论：proxy 全局替换保持现状合理；可能后续考虑 error 模式按消息位置分级。

## ChannelGroupPicker 新增 channelsOnly 模式（禁止选渠道，只能选 Key）

用户反馈：模型测试页预设下拉与编辑聚合模型 dialog 的渠道选择视觉不一致；要求统一用 ChannelGroupPicker 组件，并给该组件加新约束——**禁止选渠道级（点组标题），只能选具体 Key**。

实现：
- `ChannelGroupPicker.vue`：
  - 新增 prop `channelsOnly?: boolean`（默认 false）。开启时：
    - 自动路由按钮隐藏（`effectiveAllowAuto = allowAuto && !channelsOnly`）
    - 组标题改为静态 div（非 button，无 hover/cursor 反馈），仅作分组标签展示
    - `isGroupSelected` 永远返回 false（视觉上整组不会高亮）
    - `toggleGroup` 直接 return（点组标题不响应）
    - `applySelection` 改写：禁渠道级 → channel_base_url/channel_base_urls 始终清空；空选 = channel_ids 空（无自动路由兜底）
    - `multiToggleKey/multiToggleGroup` 在 channelsOnly 模式下也走单 Key toggle 逻辑
  - vertical / horizontal 两种布局的组标题都做了对应处理（template `v-if="channelsOnly"` 分支）
  - 旧 import 不删（RiCheckLine/RiKey2Line/RiStackLine 仍用于非 channelsOnly 路径），注释说明 channelsOnly 的语义

- `ModelTestView.vue` 预设下拉：
  - 接入 ChannelGroupPicker（vertical、`channels-only` + `multi-select=false` + `allow-auto=false` + `single-channel-group=true`），统一视觉与编辑聚合模型 dialog
  - 上方保留「Loadout 自带 API」SK key 段（不属于 channel 列表，由独立 `/api/keys` 接口管理）
  - 新增 `presetSelection: ChannelSelection` reactive 桥接 picker 状态 ↔ config.channelId：
    - watch config.channelId 变化 → 同步到 picker（重入防护 `presetFromConfig` flag 避免循环）
    - watch presetSelection.channel_ids.length 变化 → 触发 importChannel（外部同步的赋值不算，靠 flag 过滤）
    - 长度归零（用户点掉唯一选中）时清空 config.channelId
  - SK keys 段选择走原 chooseBuiltinKey，与 picker 互不干扰

验证：vue-tsc 0 错误、vite build ✓（NODE_OPTIONS="--use-system-ca" 绕过 WorkBuddy 删除拦截）。Chrome MCP 实测：popover 点开后只显示 Key tags、组标题为静态标签不可点、无「自动路由」按钮；单选 + 跨组均正常。

## 模型测试 Messages 列表允许为空（用户反馈）

用户反馈：「Messages」列表里的最后一条也需要支持删除，整个列表要支持为空（不需要至少保留 1 条占位）。

实现：`ModelTestView.vue`
- `removeMessage(index)`：去掉 `if (messages.value.length > 1)` 守卫，允许删到空。
- 模板删除按钮：去掉 `:disabled="messages.length === 1"`。
- send() 不需要改——`messages.value.filter((m) => m.content.trim()).map(...)` 已处理空数组，自然把右侧 draft 作为唯一一条 user message 发出去。
- 模板空态提示 `<p v-if="!messages.length">暂无消息...</p>` 已经存在，无需新增。

验证：vue-tsc 0 错误；Chrome MCP 实测：1 条 → 点删除 → 0 条 → 显示「暂无消息」占位。

## UnifyAI 执行方式改为 npx（用户调试成功，废弃本地脚本）

用户反馈：`npx unifyai@latest -y [...args]` 已调试成功（npx 自动拉取包，只需 node 环境），以后不再走本地仓库脚本。
- `plugins/unifyai/service.go`：`resolveCmd()` 从「UnifyaiDir / 本地仓库 src/cli.mjs / 全局 unifyai」三级查找改为统一 `npx -y unifyai@latest`（LookPath npx + Windows APPDATA/nodejs npx.cmd 兜底）；删除 `nodeCmd()`；`displayCmd()` 固定返回 `npx -y unifyai@latest`；包注释更新；移除 config import。
- `core/config/config.go`：删除死配置 `UnifyaiDir`（LOADOUT_UNIFYAI_DIR）定义与 Load 赋值（仅 service.go 一处消费，已无引用）。
- 验证：go build ./plugins/unifyai/... ./core/config/... ✓、go test ./plugins/unifyai/ 全绿 ✓。
- 约定：以后要跑 unifyai 一律 `npx -y unifyai@latest <args>`，不执行脚本。

## UnifyAI npx 参数顺序修正（-y 必须在包名前，用户报错）

用户执行报 `error: unknown option '-y'` + npx 每次弹 "Ok to proceed? (y)"。实测复现：
- `npx unifyai@latest -y --platforms opencode --dry-run` → **报错**（-y 在包名后被 npx 当作 unifyai 参数传入，unifyai 拒绝）；`--version` 不报是因为 unifyai 对 version 提前处理。
- `npx -y unifyai@latest --platforms opencode --dry-run` → **正常**（npx 消费 -y 跳过确认）。
- 根因：**npx 的 `-y`/`--yes` 选项必须放在包名【前面】**，放后面会被传给目标 CLI。
- 修复：service.go resolveCmd/displayCmd 的 base 改为 `["-y", "unifyai@latest"]`；注释记录该坑。go build + test 全绿。
- lesson：npx 选项（-y/--package/--call）一律放包名前；目标 CLI 的选项放包名后。npx 弹确认是因为 @latest 每次检查 registry、缓存未命中时询问是否下载。

## UnifyAI ③ 卡片：MCP 工具「编辑」（表单/JSON 双模式，用户澄清页面）

- 用户澄清：要的是 **UnifyAI 同步页 ③ MCP 同步过滤卡片**的编辑，不是 MCP 管理页（integrations）。先把误改的 McpPanel.vue / useMcpManagement.ts git checkout 回退。
- 实现（UnifyaiPanel.vue 单文件）：
  - ③ CardHeader 按钮组：`添加 MCP 工具` + 右侧新增`编辑`（outline，无服务器时 disabled）
  - 点编辑 → 选择服务器弹窗（Select）→ 打开编辑弹窗（默认 JSON 模式）
  - 编辑弹窗 Tabs：表单（同手动添加布局：name/type/enabled/command+env 或 url+headers）/ JSON（textarea 显示该服务器 mcp.json 条目完整 JSON，可直接改，保存校验后写回）
  - 切换模式双向同步：表单→JSON 用 buildEditPayload 生成；JSON→表单 syncEditJsonToForm 解析回填，解析失败 toast 阻止切换
  - 保存：JSON 模式 JSON.parse 校验 + 必填校验（name/command/url）→ applyEdit 替换列表项（改名时迁移 matrix/disabledServers 键）→ persistMcpServers()（后端全量 PUT /api/unifyai/mcp-servers）
  - addEnvRow/removeEnvRow 参数化（addForm/editForm 共用）
  - 顺带修：确认弹窗 `@click="confirmOpen = false; execute(false)"` 多语句被 prettier 拆多行后 rolldown-vite 编译报错（Unexpected token）→ 抽 confirmAndExecute() 函数（全项目 @click 无分号多语句先例，内联多语句会被 prettier+rolldown 搞炸）
- 验证：vue-tsc 0 错误；vite build ✓；Chrome MCP 实测：编辑按钮位置/选择弹窗/JSON 默认模式/表单回填/JSON 改 url 保存→后端 mcp.json 生效/保存后弹窗自动关闭/外部 API 改数据后需刷新页面（前端内存不同步）；测试数据已恢复（ddd url 原值、node_env 无损）。
- lesson：**rolldown-vite + prettier 组合下，模板内联多语句（分号/换行分隔）会编译失败，一律抽成函数**；外部改后端数据后前端要刷新才同步。

## UnifyAI ③ MCP 行内编辑按钮（用户二次澄清位置）

用户反馈：上轮理解错位置——编辑按钮要加在 **ExcludeMatrix 每行右侧**（删除按钮旁边），不是 ③ 卡片头部的工具栏"编辑"。
- **回退** UnifyaiPanel 工具栏的"编辑"按钮 + 选择服务器弹窗（pickDialogOpen/pickName/openPickEdit/confirmPickEdit 选择 Dialog）
- **ExcludeMatrix.vue**：
  - import RiEditLine；emit 新增 `edit: [server: McpServerInfo]`
  - 删除按钮**右侧**加编辑图标按钮（size-7，hover:text-foreground，tooltip「编辑」），@click="emit('edit', server)"
- **UnifyaiPanel.vue**：
  - 编辑入口改为 `openEditServer(server)`（行内点击直接打开编辑弹窗，无中间选服务器步骤）
  - ExcludeMatrix 加 `@edit="openEditServer"`
  - 编辑弹窗/状态/editMode/editJsonDraft/editForm/fillEditForm/buildEditPayload/applyEdit/saveEditServer 等保持不动
- 验证：vue-tsc 0 错误；vite build ✓；Chrome MCP 实测：ddd 行"编辑 ddd"在删除按钮右侧、点击直接打开 JSON 弹窗、默认 JSON 模式、改 url 保存→后端 mcp.json 生效、弹窗自动关闭；测试数据已恢复（ddd url 原值、node_env 无损）。
- **lesson**：用户说"在 X 右侧添加 Y"时，先确认 X 的具体位置（表格行内 vs 卡片头部 vs 其他容器），再动手；不要凭语义猜测。

## UnifyAI ③：补回「全局编辑 mcp.json」（用户第三次澄清，两个编辑都要）

用户：要的是**全局编辑（整个 mcp.json）+ 行内编辑两个并存**，之前全局编辑被误删。
- ③ CardHeader 右侧按钮组：`添加 MCP 工具` + 新增`编辑 mcp.json`（outline，savingMcp 时禁用；放在添加按钮右侧）
- 全局编辑弹窗（jsonFileDialogOpen/jsonFileDraft/savingJsonFile）：
  - 打开时载入 `JSON.stringify(allServers, null, 2)`（整个服务器列表 = mcp.json 内容，后端 GET/PUT 都是 {servers} 数组全量）
  - textarea 大字编辑 + 顶部「N 个服务器」计数 badge（serverCountInDraft 安全解析，坏 JSON 返回 0 不崩）
  - 保存 saveJsonFile()：JSON.parse 校验数组 → 逐项校验（name 必填、local 有 command、remote 有 url）→ allServers 全量替换 → **重建 matrix**（保留仍在列表中的服务器原有排除配置，新条目补占位）→ **重建 disabledServers**（只保留仍在列表中且 enabled===false 的）→ persistMcpServers() 全量 PUT → 关弹窗
  - 后端无需改动（复用 GET/PUT /api/unifyai/mcp-servers）
- 行内编辑（每行删除右侧编辑按钮 + 单条 JSON/表单弹窗）保持不变
- 验证：vue-tsc 0 错误；vite build ✓；Chrome MCP 实测：头部两按钮并排、全局编辑弹窗显示完整数组（2 个服务器）、追加 tmp-test-edit 保存→后端 mcp.json 3 个→UI matrix 自动补新行→外部 API 恢复→刷新后 2 个一致；弹窗保存后自动关闭
- **lesson**：用户反复澄清同一按钮位置时，最稳妥是「两个都要」——全局编辑（整文件）+ 行内编辑（单条）并存，不互斥。
- 按钮文案按用户要求改：③ 头部全局编辑按钮「编辑 mcp.json」→「编辑」（弹窗标题保留「编辑 mcp.json（全局）」）。


